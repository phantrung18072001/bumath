---
phase: 04-student-learning-submission
plan: 05
type: execute
wave: 5
depends_on: ["04-02", "04-03", "04-04"]
files_modified: []
autonomous: false
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "All student flows work on 375px viewport without horizontal scroll"
    - "All interactive elements have minimum 48x48px tap targets"
    - "Full learning flow works end-to-end: course list -> lesson -> mark complete -> submit"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete student learning and submission experience.

Purpose: Human verifies that the UI renders correctly on mobile and desktop, all interactions work as expected, and the full flow is coherent before marking the phase as done.
Output: Confirmation that all Phase 4 requirements are met visually and functionally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-student-learning-submission/04-CONTEXT.md
@.planning/phases/04-student-learning-submission/04-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of student learning portal</name>
  <files>src/pages/student/CoursesPage.tsx, src/pages/student/CourseDetailPage.tsx, src/components/student/SubmissionArea.tsx</files>
  <action>
CHECKPOINT: Human verifies the complete student learning portal.

What was built:
1. StudentLayout — compact header with logo, student name, logout button
2. CoursesPage (/courses) — enrolled course list with card grid, progress bars, grade badges
3. CourseDetailPage (/courses/:courseId) — sidebar/tab layout with chapter tree, YouTube embed, assignment viewer, mark complete, photo submission

Prerequisites: Ensure Supabase has Phase 4 migration applied (lesson_progress + submissions tables, RLS, storage bucket). Have a test student account that is approved and enrolled in at least one course with lessons.

Desktop verification (1440px):
1. Log in as approved student -> verify redirect to /courses
2. Verify course cards show in 2-column grid with grade badges and progress bars
3. Click a course -> verify sidebar layout: left sidebar (280px) with chapter tree, right content area
4. Verify YouTube video plays in 16:9 aspect ratio embed
5. Click a lesson with assignment -> verify "De bai: Xem file" opens new tab
6. Click "Danh dau da xem" -> verify button disables to "Da xem" immediately (optimistic)
7. Verify sidebar icon changes from circle to checkmark for completed lesson
8. Verify progress bar updates
9. Select photo for submission -> verify preview thumbnail appears
10. Click "Nop bai" -> verify loading state "Dang xu ly..." then success
11. Verify submitted image thumbnail visible with "Da nop" badge

Mobile verification (375px in DevTools):
1. Verify /courses shows 1-column grid
2. Verify course detail shows 2 tabs: "Noi dung" / "Muc luc"
3. Verify tab switching does not cause page reload
4. Verify all buttons are at least 48px tall (use DevTools element inspector)
5. Verify no horizontal scroll on any page
6. Verify file input offers camera capture option

Edge cases:
- Empty state: student with no enrollments sees "Chua co khoa hoc nao" message
- Course with no lessons shows "Khoa hoc chua co bai giang" message
- Breadcrumb "Khoa hoc cua toi" navigates back to /courses

Resume: Type "approved" if all checks pass, or describe specific issues to fix.
  </action>
  <verify>Human visual inspection — no automated command</verify>
  <done>All 6 Phase 4 success criteria from ROADMAP.md are confirmed by human inspection.</done>
</task>

</tasks>

<verification>
Human visual and functional verification of the complete Phase 4 student experience.
</verification>

<success_criteria>
- All 6 Phase 4 success criteria from ROADMAP.md are verified:
  1. Student sees only enrolled courses after login
  2. Student can watch YouTube video without leaving app
  3. Student can mark lesson complete and progress bar updates
  4. Student can upload compressed photo as submission
  5. Assignment shows clear status: Chua nop / Da nop / Da cham
  6. All interactions work on 375px mobile viewport with 48px tap targets
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-learning-submission/04-05-SUMMARY.md`
</output>
