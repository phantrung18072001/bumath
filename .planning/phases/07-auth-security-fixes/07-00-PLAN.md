---
phase: 07-auth-security-fixes
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/pages/Login.test.tsx
  - src/components/admin/AdminLayout.test.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Test stub files exist for Login.tsx and AdminLayout.tsx"
    - "Test stubs contain empty describe blocks for later implementation"
  artifacts:
    - path: "src/pages/Login.test.tsx"
      provides: "Test stub for Login component (AUTH-04 verification)"
      contains: "describe('Login'"
    - path: "src/components/admin/AdminLayout.test.tsx"
      provides: "Test stub for AdminLayout component (AUTH-03 verification)"
      contains: "describe('AdminLayout'"
  key_links: []
---

<objective>
Create test stub files for Phase 7 components before implementation.

Purpose: Nyquist compliance — test files must exist before implementation tasks so that `<verify>` commands in later plans have valid targets. Wave 0 ensures the test infrastructure is in place.

Output: Two empty test stub files with describe blocks that later plans will populate with actual tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-auth-security-fixes/07-RESEARCH.md

@src/components/student/StudentLayout.test.tsx (test stub pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Login.test.tsx stub</name>
  <files>src/pages/Login.test.tsx</files>
  <read_first>
    - src/pages/Login.tsx
    - src/components/student/StudentLayout.test.tsx
  </read_first>
  <action>
Create `src/pages/Login.test.tsx` with an empty test stub following the dynamic import pattern from StudentLayout.test.tsx.

The stub should:
1. Mock `@/contexts/AuthContext` using `vi.mock`
2. Mock `react-router-dom` to provide `useNavigate` mock
3. Include an empty `describe('Login', () => {})` block
4. Include a placeholder test that can be filled in later

```typescript
import { vi } from 'vitest'

// Mock AuthContext
vi.mock('@/contexts/AuthContext', () => ({
  useAuth: vi.fn(),
}))

// Mock react-router-dom
vi.mock('react-router-dom', async () => {
  const actual = await vi.importActual('react-router-dom')
  return { ...actual, useNavigate: vi.fn() }
})

describe('Login', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  // Test cases will be added in Plan 07-02
  it.todo('redirects to /pending when approval_status is pending')
  it.todo('redirects to /courses when role is student and approved')
  it.todo('redirects to /admin/users when role is admin and approved')
})
```

This stub enables Plan 07-02 to add real test implementations.
  </action>
  <acceptance_criteria>
    - `src/pages/Login.test.tsx` exists
    - File contains `describe('Login'`
    - File contains `vi.mock('@/contexts/AuthContext'`
    - File contains `vi.mock('react-router-dom'`
    - File contains `it.todo('redirects to /pending`
  </acceptance_criteria>
  <verify>
    <automated>test -f src/pages/Login.test.tsx && grep -q "describe('Login'" src/pages/Login.test.tsx && grep -q "vi.mock('@/contexts/AuthContext'" src/pages/Login.test.tsx && echo "Login.test.tsx stub valid"</automated>
  </verify>
  <done>Login.test.tsx stub exists with mocks and empty describe block</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminLayout.test.tsx stub</name>
  <files>src/components/admin/AdminLayout.test.tsx</files>
  <read_first>
    - src/components/admin/AdminLayout.tsx
    - src/components/student/StudentLayout.test.tsx
  </read_first>
  <action>
Create `src/components/admin/AdminLayout.test.tsx` with an empty test stub following the StudentLayout.test.tsx pattern.

The stub should:
1. Mock `@/contexts/AuthContext` using `vi.mock`
2. Include an empty `describe('AdminLayout', () => {})` block
3. Include placeholder tests for logout functionality

```typescript
import { vi } from 'vitest'

// Mock AuthContext
vi.mock('@/contexts/AuthContext', () => ({
  useAuth: vi.fn().mockReturnValue({
    profile: { id: 'admin-1', full_name: 'Admin User', role: 'admin' },
    loading: false,
    signOut: vi.fn(),
  }),
}))

describe('AdminLayout', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  // Test cases will be added in Plan 07-02
  it.todo('renders logout button with text "Đăng xuất"')
  it.todo('clicking logout calls signOut()')
  it.todo('renders all nav items')
})
```

This stub enables Plan 07-02 to add real test implementations.
  </action>
  <acceptance_criteria>
    - `src/components/admin/AdminLayout.test.tsx` exists
    - File contains `describe('AdminLayout'`
    - File contains `vi.mock('@/contexts/AuthContext'`
    - File contains `it.todo('renders logout button`
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/admin/AdminLayout.test.tsx && grep -q "describe('AdminLayout'" src/components/admin/AdminLayout.test.tsx && grep -q "vi.mock('@/contexts/AuthContext'" src/components/admin/AdminLayout.test.tsx && echo "AdminLayout.test.tsx stub valid"</automated>
  </verify>
  <done>AdminLayout.test.tsx stub exists with mocks and empty describe block</done>
</task>

</tasks>

<verification>
All Wave 0 stubs created:
```bash
ls -la src/pages/Login.test.tsx src/components/admin/AdminLayout.test.tsx
```

Quick syntax check:
```bash
yarn test src/pages/Login.test.tsx src/components/admin/AdminLayout.test.tsx --run 2>&1 | head -20
```
</verification>

<success_criteria>
- [ ] `src/pages/Login.test.tsx` exists with describe block and mocks
- [ ] `src/components/admin/AdminLayout.test.tsx` exists with describe block and mocks
- [ ] Both files can be parsed by Vitest without syntax errors
- [ ] Wave 0 is complete — later plans have valid test file targets
</success_criteria>

<output>
After completion, create `.planning/phases/07-auth-security-fixes/07-00-SUMMARY.md`
</output>
