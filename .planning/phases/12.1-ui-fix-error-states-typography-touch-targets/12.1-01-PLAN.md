---
phase: 12.1-ui-fix-error-states-typography-touch-targets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/admin/GradingPage.tsx
  - src/pages/admin/CoursesPage.tsx
  - src/pages/admin/UsersPage.tsx
autonomous: true
requirements:
  - DS-01  # Typography conformance
  - ADMIN-UI-05  # GradingPage mobile UX
must_haves:
  truths:
    - "GradingPage 'Thêm ảnh phản hồi' button has 48px touch target"
    - "GradingPage Loader2 container has aria-label for screen readers"
    - "CoursesPage and UsersPage empty-state headlines use text-sm (not text-base)"
    - "CoursesPage course title cell uses font-normal (not font-medium)"
  artifacts:
    - path: "src/pages/admin/GradingPage.tsx"
      provides: "Touch target fix + Loader2 accessibility"
      contains: "min-h-[48px]"
    - path: "src/pages/admin/CoursesPage.tsx"
      provides: "Typography fixes: text-sm headlines, font-normal title"
    - path: "src/pages/admin/UsersPage.tsx"
      provides: "Typography fixes: text-sm headlines"
  key_links:
    - from: "GradingPage.tsx:276"
      to: "UI-SPEC D-13"
      via: "min-h-[48px] touch target rule"
    - from: "CoursesPage.tsx:189,198"
      to: "UI-SPEC typography contract"
      via: "text-sm body size (14px)"
---

<objective>
Fix touch targets, typography, and accessibility in GradingPage, CoursesPage, and UsersPage.

Purpose: Address Wave 1 violations from Phase 12 UI Review — these files have no dependencies on each other and can be fixed in parallel.

Output:
- GradingPage: 48px touch target on "Thêm ảnh phản hồi" button + aria-label on Loader2 container
- CoursesPage: text-sm on 2 empty-state headlines + font-normal on course title cell
- UsersPage: text-sm on 2 empty-state headlines
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12.1-ui-fix-error-states-typography-touch-targets/12.1-CONTEXT.md
@.planning/phases/12-admin-detail-pages/12-UI-REVIEW.md
@.planning/phases/12-admin-detail-pages/12-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GradingPage touch target and accessibility</name>
  <files>src/pages/admin/GradingPage.tsx</files>
  <read_first>
    - src/pages/admin/GradingPage.tsx (current state — line 276 has min-h-[40px], lines 119-125 have Loader2)
    - .planning/phases/12.1-ui-fix-error-states-typography-touch-targets/12.1-CONTEXT.md (D-07, D-08, D-13)
  </read_first>
  <action>
1. **Fix touch target (line 276):** Change `className="min-h-[40px]"` to `className="min-h-[48px]"` on the "Thêm ảnh phản hồi" Button component. This is the only min-h-[40px] instance in the file.

2. **Add aria-label to Loader2 container (lines 119-125):** The loading spinner container currently has no aria-label. Change:
   ```tsx
   <div className="container mx-auto py-8 max-w-5xl flex justify-center items-center min-h-64">
     <Loader2 className="h-8 w-8 animate-spin text-primary" />
   </div>
   ```
   to:
   ```tsx
   <div className="container mx-auto py-8 max-w-5xl flex justify-center items-center min-h-64" aria-label="Đang tải...">
     <Loader2 className="h-8 w-8 animate-spin text-primary" />
   </div>
   ```

3. **Add mobile image area overflow + padding (lines 159-200):** Add `overflow-y-auto pb-24 lg:pb-0` to the image carousel container div. Find the div at line 159 with `className="flex-1 min-w-0"` and change to `className="flex-1 min-w-0 overflow-y-auto pb-24 lg:pb-0"`.
  </action>
  <verify>
    <automated>grep -n "min-h-\[48px\]" src/pages/admin/GradingPage.tsx | grep -v "# should find line 276"</automated>
    <automated>grep -n 'aria-label="Đang tải..."' src/pages/admin/GradingPage.tsx | grep -c "." | grep -q "^1$" && echo "PASS: aria-label found once"</automated>
    <automated>grep -n "overflow-y-auto pb-24" src/pages/admin/GradingPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "min-h-\[40px\]" src/pages/admin/GradingPage.tsx` returns 0
    - `grep -c "min-h-\[48px\]" src/pages/admin/GradingPage.tsx` returns 7 or more (was 6, now 7)
    - `grep -n 'aria-label="Đang tải..."' src/pages/admin/GradingPage.tsx` returns at least 1 match in the Loader2 container area
    - `grep -n "overflow-y-auto pb-24" src/pages/admin/GradingPage.tsx` returns at least 1 match
  </acceptance_criteria>
  <done>GradingPage has 48px touch target on "Thêm ảnh phản hồi" button, Loader2 container has aria-label, mobile image area has overflow-y-auto and pb-24</done>
</task>

<task type="auto">
  <name>Task 2: Fix CoursesPage typography violations</name>
  <files>src/pages/admin/CoursesPage.tsx</files>
  <read_first>
    - src/pages/admin/CoursesPage.tsx (current state — lines 189,198 have text-base, line 221 has font-medium)
    - .planning/phases/12.1-ui-fix-error-states-typography-touch-targets/12.1-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
1. **Fix empty-state headline 1 (line 189):** Change `className="text-base font-semibold text-foreground mb-1"` to `className="text-sm font-semibold text-foreground mb-1"`. This is the "Chưa có khóa học nào" headline.

2. **Fix empty-state headline 2 (line 198):** Change `className="text-base font-semibold text-foreground mb-1"` to `className="text-sm font-semibold text-foreground mb-1"`. This is the "Không tìm thấy kết quả" headline.

3. **Fix course title cell weight (line 221):** Change `<TableCell className="font-medium">` to `<TableCell className="font-normal">`. The course title should use body weight (400), not medium (500).
  </action>
  <verify>
    <automated>grep -c "text-base" src/pages/admin/CoursesPage.tsx | grep -q "^0$" && echo "PASS: no text-base"</automated>
    <automated>grep -n "font-medium" src/pages/admin/CoursesPage.tsx | grep -c "." | grep -q "^0$" && echo "PASS: no font-medium"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "text-base" src/pages/admin/CoursesPage.tsx` returns 0
    - `grep -c "font-medium" src/pages/admin/CoursesPage.tsx` returns 0
    - `grep -n "text-sm font-semibold" src/pages/admin/CoursesPage.tsx` returns matches on lines 189 and 198
    - `grep -n 'TableCell className="font-normal"' src/pages/admin/CoursesPage.tsx` returns a match around line 221
  </acceptance_criteria>
  <done>CoursesPage empty-state headlines use text-sm and course title cell uses font-normal</done>
</task>

<task type="auto">
  <name>Task 3: Fix UsersPage typography violations</name>
  <files>src/pages/admin/UsersPage.tsx</files>
  <read_first>
    - src/pages/admin/UsersPage.tsx (current state — lines 186,191 have text-base)
    - .planning/phases/12.1-ui-fix-error-states-typography-touch-targets/12.1-CONTEXT.md (D-04)
  </read_first>
  <action>
1. **Fix empty-state headline 1 (line 186):** Change `className="text-base font-semibold text-foreground mb-1"` to `className="text-sm font-semibold text-foreground mb-1"`. This is the "Chưa có tài khoản nào" headline.

2. **Fix empty-state headline 2 (line 191):** Change `className="text-base font-semibold text-foreground mb-1"` to `className="text-sm font-semibold text-foreground mb-1"`. This is the "Không tìm thấy kết quả" headline.
  </action>
  <verify>
    <automated>grep -c "text-base" src/pages/admin/UsersPage.tsx | grep -q "^0$" && echo "PASS: no text-base"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "text-base" src/pages/admin/UsersPage.tsx` returns 0
    - `grep -n "text-sm font-semibold" src/pages/admin/UsersPage.tsx` returns matches on lines 186 and 191
  </acceptance_criteria>
  <done>UsersPage empty-state headlines use text-sm (14px) instead of text-base (16px)</done>
</task>

</tasks>

<verification>
Wave 1 completion checks:

```bash
# Typography: No text-base in CoursesPage or UsersPage
grep -c "text-base" src/pages/admin/CoursesPage.tsx  # expect 0
grep -c "text-base" src/pages/admin/UsersPage.tsx     # expect 0

# Typography: No font-medium in CoursesPage
grep -c "font-medium" src/pages/admin/CoursesPage.tsx  # expect 0

# Touch targets: No min-h-[40px] in GradingPage
grep -c "min-h-\[40px\]" src/pages/admin/GradingPage.tsx  # expect 0

# Accessibility: aria-label on Loader2 container
grep -n 'aria-label="Đang tải..."' src/pages/admin/GradingPage.tsx  # expect match

# Mobile: overflow-y-auto pb-24 on image area
grep -n "overflow-y-auto pb-24" src/pages/admin/GradingPage.tsx  # expect match

# Build check
yarn build 2>&1 | tail -5
```
</verification>

<success_criteria>
- All 4 typography violations fixed (2 in CoursesPage, 2 in UsersPage, 1 font-medium in CoursesPage)
- GradingPage touch target upgraded to 48px
- GradingPage Loader2 container has aria-label="Đang tải..."
- GradingPage mobile image area has overflow-y-auto and pb-24
- `yarn build` passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-ui-fix-error-states-typography-touch-targets/12.1-01-SUMMARY.md`
</output>
