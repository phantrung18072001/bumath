---
status: complete
phase: 12.1-ui-fix-error-states-typography-touch-targets
source: [12.1-01-SUMMARY.md, 12.1-02-SUMMARY.md]
started: 2026-05-02T18:42:00+07:00
updated: 2026-05-02T18:52:00+07:00
---

## Current Test

[testing complete]

## Tests

### 1. GradingPage "Thêm ảnh phản hồi" touch target
expected: On a mobile viewport (or with DevTools mobile emulation), open any grading submission page. The "Thêm ảnh phản hồi" button near the teacher image upload section should be visibly taller and easy to tap — at least 48px high (previously it was 40px and felt too small to hit reliably).
result: pass

### 2. GradingPage mobile image area scrolling
expected: On a mobile viewport (< 1024px), open a grading page that has teacher feedback images. The image area on the left side should scroll vertically if content overflows, and the fixed bottom bar should NOT overlap or cut off the last image. Content should be fully accessible by scrolling.
result: pass

### 3. CoursesPage empty-state text size
expected: On the Courses admin page, either search for something with no results or view when no courses exist. The headline text ("Chưa có khóa học nào" or "Không tìm thấy kết quả") should appear at normal body text size — it should not look noticeably larger than the surrounding descriptive text below it.
result: pass

### 4. UsersPage empty-state text size
expected: On the Users admin page, search for something with no results. The headline text ("Không tìm thấy kết quả") should appear at normal body text size — consistent with the CoursesPage empty state.
result: pass

### 5. SubmissionsPage error state
expected: When the Submissions page fails to load data (simulate by disabling network or temporarily breaking the API), it should show "Không thể tải dữ liệu. Vui lòng thử lại." in red text instead of showing an endless loading spinner or blank screen.
result: pass

### 6. SubmissionsPage count badge color
expected: On the Submissions admin page, the badge showing the submission count in the header (e.g. "12") should use a neutral grey/muted style — NOT orange. It should blend with the page without drawing attention.
result: pass

### 7. ChaptersPage error state
expected: When the Chapters page fails to load data, it should show "Không thể tải dữ liệu. Vui lòng thử lại." in red text instead of a blank or perpetually loading state.
result: pass

### 8. ChaptersPage delete dialog wording
expected: When clicking the delete icon on any chapter row and confirming deletion, the dialog title should read "Xóa chuyên đề?" — with a question mark at the end. (Previously it was missing the "?")
result: pass

### 9. LessonsPage error state
expected: When the Lessons page fails to load data, it should show "Không thể tải dữ liệu. Vui lòng thử lại." in red text instead of a blank or perpetually loading state.
result: pass

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0

## Gaps
