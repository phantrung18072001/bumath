---
phase: 6
slug: ux-polish
status: draft
shadcn_initialized: true
preset: "style=default, baseColor=slate, cssVariables=true"
created: 2026-04-28
---

# Phase 6 — UX Polish: UI Design Contract

> Visual and interaction contract for Phase 6 frontend work. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Source authority:** All token values sourced directly from `src/index.css` CSS variable declarations. All component patterns sourced from existing `.tsx` files via direct inspection.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | `components.json` detected |
| Preset | style=default, baseColor=slate, cssVariables=true | `components.json` |
| Component library | Radix UI (via shadcn/ui) | installed |
| Icon library | Lucide React (already installed) | `package.json` |
| Font | "Be Vietnam Pro" (400–900) | `src/index.css` @import |
| Dark mode | `class` strategy | `tailwind.config.ts` |

> **Registry:** shadcn official only. No third-party registries. Safety vetting: not required.

---

## Design Tokens — Color

All values from `src/index.css`. Use CSS variables, not raw HSL values, in code.

### Light Mode

| Token | HSL Value | Resolved Color | Semantic Role |
|-------|-----------|---------------|---------------|
| `--background` | `30 50% 98%` | Warm off-white cream | Page background |
| `--foreground` | `20 30% 12%` | Dark warm near-black | Body text |
| `--card` | `0 0% 100%` | Pure white | Card surfaces, dialogs |
| `--card-foreground` | `20 30% 12%` | Same as foreground | Card text |
| `--primary` | `24 95% 53%` | Orange | CTAs, logo text, links, ring |
| `--primary-foreground` | `0 0% 100%` | White | Text on primary buttons |
| `--secondary` | `200 80% 50%` | Blue/cyan | **DO NOT use for progress track** |
| `--muted` | `30 20% 94%` | Light warm gray | Progress track, skeleton, subtle backgrounds |
| `--muted-foreground` | `20 10% 45%` | Medium gray | Captions, placeholder, secondary labels |
| `--accent` | `45 100% 55%` | Warm gold/yellow | Bell badge, highlight chips — see accent reserved list |
| `--accent-foreground` | `20 30% 12%` | Dark warm | Text on accent elements |
| `--destructive` | `0 84% 60%` | Red | Delete actions, error alerts only |
| `--border` | `30 15% 88%` | Warm light border | All borders, dividers |
| `--ring` | `24 95% 53%` | Orange | Focus rings — matches primary |
| `--sidebar-background` | `0 0% 98%` | Near-white | Sidebar/lesson panel surface |

### Dark Mode (class="dark")

| Token | HSL Value | Resolved Color | Change from Light |
|-------|-----------|---------------|-------------------|
| `--background` | `20 20% 8%` | Near-black warm | — |
| `--card` | `20 20% 12%` | Dark warm card | — |
| `--muted` | `20 15% 18%` | Dark warm muted | — |
| `--secondary` | `200 80% 45%` | Slightly darker blue | Still not for progress tracks |
| `--border` | `20 15% 20%` | Dark border | — |
| `--primary` | `24 95% 53%` | Orange (unchanged) | Same |

---

## Color Contract (60/30/10)

| Role | Token(s) | % | Specific Usage |
|------|----------|---|----------------|
| Dominant surface | `bg-background` | 60% | Page body, main content area |
| Secondary surface | `bg-card`, `bg-sidebar-background`, `bg-muted` | 30% | Cards, course cards, sidebar, filter bars, skeleton states |
| Accent | `text-primary`, `bg-primary`, `bg-accent` | 10% | Reserved list below |
| Destructive | `bg-destructive` | < 1% | Delete/reject buttons and error Alert components only |

**Accent reserved for (explicit list — nowhere else):**
1. Primary CTA buttons (`bg-primary` orange)
2. Logo "BuMath" text (`text-primary`)
3. Active nav link indicator / hover underline
4. Progress indicator bar fill (`bg-primary`)
5. Bell badge for unread notification count (`bg-accent` yellow)
6. Grade badge chips (use existing `GRADE_BADGE` className values)
7. Focus rings (`ring-ring`)

**Progress track specifically:** MUST use `bg-muted` (warm gray). MUST NOT use `bg-secondary` (blue). This is the primary visual bug fix in Phase 6.

---

## Spacing Scale

Declared values (Tailwind 8-point scale):

| Token | px Value | Tailwind Class | Usage in This Phase |
|-------|----------|----------------|---------------------|
| xs | 4px | `gap-1`, `p-1` | Icon-to-text gaps (logo gap, lock icon gap in preview) |
| sm | 8px | `gap-2`, `p-2` | Nav link internal padding, badge spacing |
| md | 16px | `gap-4`, `p-4` | Card padding, filter bar spacing, header px |
| lg | 24px | `gap-6`, `p-6` | Page content padding (mobile `p-6`) |
| xl | 32px | `gap-8`, `p-8` | Page content padding (desktop `md:p-8`) |
| 2xl | 48px | `h-12`, `min-h-[48px]` | Header height, touch target minimum |
| 3xl | 64px | `py-16` | Loader centered vertical space |

**Exceptions:**
- Touch targets: all interactive elements MUST be ≥ 48×48px (UX-02). Nav links use `px-3 py-2` + min-h enforcement via header flex centering.
- Logo image: 32×32px (`h-8 w-8`) — intentionally smaller than touch target, surrounded by 48px Link hit area.
- Filter section: `flex-wrap gap-2` — 8px gap between filter controls.

---

## Typography

Font: **"Be Vietnam Pro"** — loaded via Google Fonts CDN, weights 400/500/600/700/800/900 available.

| Role | Size | Tailwind | Weight | Weight Name | Line Height | Usage |
|------|------|----------|--------|-------------|-------------|-------|
| Body | 14px | `text-sm` | 400 | Regular | 1.5 | Table cells, card descriptions, filter labels, progress caption, nav link default text |
| Interactive | 14px | `text-sm` | 600 | Semibold | 1.4 | Nav links (`font-medium` → renders as semibold), badge text, button text, form labels |
| Heading | 20px | `text-xl` | 600 | Semibold | 1.3 | Page section headings (admin queue "Chấm bài", student "Khóa học của tôi") |
| Sub-heading | 24px | `text-2xl` | 600 | Semibold | 1.25 | Course titles on CoursesPage, Catalogue page |
| Display | 36px | `text-4xl` | 700 | Bold | 1.2 | **404 error number only — single-element exception, not in global weight scale** |

> **Declared weight scale: 400 (Regular) + 600 (Semibold) only.**
> Weight 500 (Medium) is removed — all previously "medium" interactive elements (nav links, buttons, badge text, form labels) use 600 Semibold instead.
> Weight 700 (Bold) is a single-element undeclared exception for the 404 number; it does not appear on any normal screen and is not part of the global scale.

**Rules:**
- Maximum 4 type sizes on any single screen. Never introduce a 5th.
- Maximum 2 weights per screen. Semibold (600) + Regular (400) on all screens except 404.
- Never use `font-bold` (700) except on the 404 number.
- Vietnamese text renders well at 14px and above — do not go below 14px for any user-facing copy.
- `text-muted-foreground` at `text-sm` = secondary/supporting copy (captions, empty state body, placeholder text).

---

## Component Inventory

All components below are **already installed** in `src/components/ui/`. Zero new installs required.

### Existing Components Used in Phase 6

| Component | Import Path | Phase 6 Usage |
|-----------|-------------|---------------|
| `Button` | `@/components/ui/button` | Nav logout, filter clear, grading "Chấm" action |
| `Badge` | `@/components/ui/badge` | Grade badge, enrollment status badge, pending count |
| `Card`, `CardHeader`, `CardContent`, `CardTitle` | `@/components/ui/card` | Course cards on CataloguePage, empty state card |
| `Progress` | `@/components/ui/progress` | Course completion bar (with `className="bg-muted"` override) |
| `Skeleton` | `@/components/ui/skeleton` | Loading states on CataloguePage (4 skeleton cards) |
| `Alert`, `AlertDescription` | `@/components/ui/alert` | Error states on CataloguePage |
| `Select`, `SelectContent`, `SelectItem`, `SelectTrigger`, `SelectValue` | `@/components/ui/select` | Grade, course, lesson filter dropdowns on SubmissionsPage |
| `Input` | `@/components/ui/input` | Student name text filter on SubmissionsPage |
| `Table`, `TableBody`, `TableCell`, `TableHead`, `TableHeader`, `TableRow` | `@/components/ui/table` | Existing grading queue table (unchanged) |
| `Link` (React Router) | `react-router-dom` | Logo link, nav links, course card links |

### New Components to Create

| Component | Path | Type |
|-----------|------|------|
| `CataloguePage` | `src/pages/student/CataloguePage.tsx` | Full page |

### Icons Required (Lucide React)

| Icon | Component Name | Usage | Size |
|------|----------------|-------|------|
| Lock | `Lock` | Lesson rows in preview mode (non-enrolled) | `h-4 w-4` |
| SlidersHorizontal | `SlidersHorizontal` | Filter bar icon/label on SubmissionsPage | `h-4 w-4` |
| LogOut | `LogOut` | Logout button (already present) | `h-4 w-4` |
| Loader2 | `Loader2` | Loading spinner (already present pattern) | `h-8 w-8 animate-spin` |

---

## Screen-by-Screen Interaction Contract

### Screen 1: StudentLayout Header (Navigation Fix)

**Current:** `<span>BuMath</span>` — non-interactive.
**Required:** Full clickable logo region with image + text.

```
[Logo image 32×32] [BuMath text]  |  [Khóa học của tôi]  [Khám phá]  ··· [Bell] [Đăng xuất]
|___________Link to "/"___________|  |___Link to /courses__|  |___/catalogue___|
```

**Logo link:**
- Element: `<Link to="/">` wrapping `<img>` + `<span>`
- Image: `src={${import.meta.env.BASE_URL}bumath.jpeg}`, `alt="BuMath"`, `className="h-8 w-8 rounded-lg object-cover"`
- Text: `<span className="font-semibold text-primary text-base">BuMath</span>`
- Gap between img and text: `gap-2` (8px)
- Hover: no explicit hover style needed — logo links conventionally have no hover state

**Nav links (desktop, hidden on mobile):**
- Container: `<nav className="ml-4 hidden sm:flex items-center gap-1">`
- "Khóa học của tôi" link: `to="/courses"`, `className="text-sm font-semibold px-3 py-2 rounded-lg hover:bg-muted transition-colors"`
- "Khám phá khóa học" link: `to="/catalogue"`, same className
- Active state: add `text-primary font-semibold` when on matching route (use `useLocation` or `NavLink`)
- Min touch target: `min-h-[48px]` implicit via header height + flex centering

**Mobile (< 640px):** Nav links hidden (`hidden sm:flex`). Logo + Bell + Logout remain visible.

---

### Screen 2: NotFound Page (404 Fix)

**Layout:** Full viewport centered (`flex min-h-screen items-center justify-center bg-muted`)

**Content structure:**
```
[Error number: "404"]         text-4xl font-bold text-foreground
[Message line]                text-xl text-muted-foreground
[Action link]                 text-primary underline hover:text-primary/90
```

**Copy (Vietnamese — UX-03):**
- Error number: `404`
- Message: `Trang không tìm thấy`
- Link text: `Về trang chủ`
- Link destination: role-aware — `profile?.role === 'student'` → `/courses`, else `/`

**Link element:** React Router `<Link>` (NOT `<a href>`). Prevents full page reload.

**States:**
- Auth loading: if profile not yet resolved, default link to `/` (safe fallback)
- Logged out: link to `/`

---

### Screen 3: Progress Bar Track Color Fix

**Scope:** Two call sites.

| File | Current className | Required className |
|------|------------------|--------------------|
| `src/pages/student/CoursesPage.tsx` ~line 130 | `className="h-2 mt-2"` | `className="h-2 mt-2 bg-muted"` |
| `src/components/student/LessonSidebar.tsx` ~line 31 | `className="h-2"` | `className="h-2 bg-muted"` |

**Visual result:**
- Track: `bg-muted` = `hsl(30 20% 94%)` = light warm gray (not blue)
- Indicator fill: `bg-primary` = `hsl(24 95% 53%)` = orange (unchanged)

**ARIA:** Preserve `aria-label={Tiến độ hoàn thành: ${progress}%}` on the `<Progress>` element.

**Do NOT:** Modify `src/components/ui/progress.tsx` directly (CLAUDE.md constraint). Use `className` prop override only.

---

### Screen 4: Admin Grading Queue — Filter Bar (SubmissionsPage)

**Layout:** Filter bar injected between page heading and table.

```
[Page heading: "Chấm bài"] [Badge: N bài chờ chấm]

[Filter bar — flex-wrap gap-2]
  [Grade Select]  [Course Select]  [Lesson Select]  [Student name Input]  [Clear button?]

[Table — existing layout unchanged]
```

**Filter bar container:**
- `className="flex flex-wrap gap-2 mb-6 p-4 bg-muted/50 rounded-lg border border-border"`

**Filter 1 — Grade (Lớp):**
- Component: `<Select>` with `<SelectTrigger className="w-[140px]">`
- Placeholder: `"Tất cả lớp"`
- Options: derived from `GRADE_BADGE` map — labels "Lớp 7", "Lớp 8", "Lớp 9", "Ôn chuyên"
- Empty option value: `""` (empty string = no filter)

**Filter 2 — Course (Khóa học):**
- Component: `<Select>` with `<SelectTrigger className="w-[180px]">`
- Placeholder: `"Tất cả khóa học"`
- Options: `uniqueCourses` derived with `Array.from(new Set(data.map(r => r.lessons.chapters.courses.title)))`
- Empty option value: `""` (no filter)

**Filter 3 — Lesson (Bài học):**
- Component: `<Select>` with `<SelectTrigger className="w-[180px]">`
- Placeholder: `"Tất cả bài học"`
- Options: `uniqueLessons` derived with `Array.from(new Set(data.map(r => r.lessons.title)))`
- Empty option value: `""` (no filter)

**Filter 4 — Student name (Học sinh):**
- Component: `<Input>` with `placeholder="Tìm học sinh..."`, `className="w-[180px]"`
- Behavior: partial, case-insensitive match against `profiles.full_name`
- No debounce needed — client-side filter is instant

**Result count:** Below filter bar, above table: `<p className="text-sm text-muted-foreground mb-3">Hiển thị {filteredData.length} / {data.length} bài nộp</p>`

**Empty filtered state (no results after filter):**
- Show: `<p className="text-center text-muted-foreground py-8">Không tìm thấy bài nộp nào phù hợp với bộ lọc.</p>`

**Filter clear:** All selects reset to `""` (Tất cả) when user selects the placeholder option. No explicit "Clear all" button required in MVP.

**Mobile (< 640px):** Filter bar wraps to 2 columns (`flex-wrap`). Selects stack naturally. Table scrolls horizontally (`overflow-x-auto` already present).

---

### Screen 5: Course Catalogue Page (CataloguePage) — NEW

**Route:** `/catalogue` (ProtectedRoute requiredRole="student")
**Layout:** Wrapped in `<StudentLayout>`
**Primary focal point:** Page heading `Khám phá khóa học` (text-2xl font-semibold, first rendered element, top-left of content area)

**Page structure:**
```
[Page header]
  h1: "Khám phá khóa học"                text-2xl font-semibold mb-2
  p: "Tất cả các khóa học đang có tại BuMath"  text-sm text-muted-foreground mb-6

[Course grid — same grid as CoursesPage]
  grid-cols-1 gap-6 sm:grid-cols-2       (matches existing CoursesPage layout)

  [Course card — Link → /courses/:slug]
    CardHeader:
      CardTitle: course.title            text-xl font-semibold
      Badge: grade badge                 GRADE_BADGE[course.target_grade]
      Badge: enrollment status           see enrollment badge spec below
    CardContent:
      description (if any)              text-sm text-muted-foreground (max 2 lines, truncate)
```

**Enrollment status badge (per card):**
- Enrolled: `<Badge className="bg-green-100 text-green-700 hover:bg-green-100">Đã đăng ký</Badge>`
- Not enrolled: `<Badge variant="outline" className="text-muted-foreground">Chưa đăng ký</Badge>`

**Loading state:** 4 skeleton cards — `<Skeleton className="h-40 rounded-xl" />` (identical to CoursesPage loading pattern)

**Empty state (no courses at all):**
```
<Card className="max-w-md w-full p-6 text-center mx-auto">
  <h2 className="text-xl font-semibold mb-2">Chưa có khóa học nào</h2>
  <p className="text-muted-foreground text-sm">
    Hiện tại chưa có khóa học nào. Vui lòng quay lại sau.
  </p>
</Card>
```

**Error state:**
```
<Alert variant="destructive">
  <AlertDescription>
    Không thể tải danh sách khóa học. Vui lòng làm mới trang.
  </AlertDescription>
</Alert>
```

**Course card click behavior:**
- Always navigates to `/courses/:slug`
- CourseDetailPage detects enrollment and renders preview vs. full mode (see Screen 6)

**Page padding:** `p-6 md:p-8` (matches CoursesPage)

---

### Screen 6: Course Preview Mode (CourseDetailPage — non-enrolled)

**Context:** Triggered when `isEnrolled === false` AND course data loads successfully (after RLS migration allows all approved users to query).
**Primary focal point:** Course title h1 (text-2xl font-semibold), followed immediately by the lock notice banner — together they communicate the "view-only" state within the first viewport.

**Both enrollment and chapter queries must resolve before mode decision.** Show unified loading skeleton until both are done.

**Preview mode layout:**
```
[Course header — always visible]
  h1: course.title                       text-2xl font-semibold
  Badge: grade badge
  p: course.description (if any)         text-muted-foreground text-sm

[Lock notice banner]
  bg-muted border border-border rounded-lg p-4 mb-4
  flex items-center gap-3
  [Lock icon h-5 w-5 text-muted-foreground]
  <p className="text-sm text-muted-foreground">
    "Bạn chưa đăng ký khóa học này."
  </p>

[Chapter accordion list]
  For each chapter:
    div.border.rounded-lg.p-4.mb-3
    h3: chapter.title    text-base font-semibold mb-2
    ul:
      For each lesson:
        li.flex.items-center.gap-2.py-1.5
          <Lock className="h-4 w-4 shrink-0 text-muted-foreground" />
          <span className="text-sm text-muted-foreground">{lesson.title}</span>

[Contact CTA at bottom]
  mt-6 p-4 bg-muted rounded-lg text-center
  <p className="text-sm text-muted-foreground">
    "Vui lòng liên hệ giảng viên để được đăng ký khóa học này."
  </p>
```

**Full mode (isEnrolled === true):** Existing sidebar + LessonContent layout — NO changes.

**Loading state for enrollment check:** Composite — both chapter query `isLoading` AND enrollment query `isLoading` must be false before rendering. Use existing loading spinner pattern: `<Loader2 className="h-8 w-8 animate-spin text-primary" />` centered.

**Error state (enrollment query fails):** Fail safe to **preview mode** (not full mode) — safer default.

---

## Interaction States

All interactive elements must implement all 5 states:

| Element | Default | Hover | Focus | Active | Disabled |
|---------|---------|-------|-------|--------|----------|
| Primary Button | `bg-primary text-white` | `bg-primary/90` | `ring-2 ring-ring` | `scale-[0.98]` | `opacity-50 cursor-not-allowed` |
| Ghost Button (nav, logout) | `transparent` | `bg-muted` | `ring-2 ring-ring` | `bg-muted/80` | `opacity-50` |
| Course card (Link) | `shadow-sm` | `shadow-md` | visible ring | `shadow-sm` | N/A |
| Select trigger | `bg-background border` | `border-primary` | `ring-2 ring-ring` | — | `opacity-50` |
| Input (student filter) | `bg-background border` | `border-primary` | `ring-2 ring-ring` | — | N/A |
| Nav link | `text-foreground` | `bg-muted` | `ring-2 ring-ring` | — | N/A |
| Active nav link | `text-primary font-semibold` | — | — | — | — |

**Transition:** All hover/focus transitions use `transition-colors duration-200` (Tailwind default). No custom keyframes for Phase 6 interactions.

---

## Motion & Animation

Phase 6 is a polish phase — motion must enhance, not distract.

| Element | Motion | Duration | Easing | Trigger |
|---------|--------|----------|--------|---------|
| Progress bar fill | width transition | 300ms | ease-out | On `value` prop change (handled by shadcn Progress) |
| Course card hover | `shadow-md` | 200ms (transition-shadow) | ease | `hover:shadow-md` already in CoursesPage |
| Filter dropdown open | slide-down | 150ms | ease-out | Radix Select default animation |
| Filter dropdown close | slide-up | 100ms | ease-in | Radix Select default animation |
| Page navigation | none | — | — | React Router instant navigation |
| Skeleton → content | opacity fade | 0ms | none | React Query `isLoading` → `false` (immediate swap, no crossfade needed) |

**Rule:** No new CSS keyframe animations in Phase 6. Use only Tailwind transitions and Radix UI default animations.

---

## Accessibility Contract

All Phase 6 changes must meet WCAG 2.1 AA:

| Element | Requirement | Implementation |
|---------|-------------|----------------|
| Logo link | `aria-label="Trang chủ BuMath"` | Add to `<Link>` wrapping logo |
| Nav links | Visible text labels (no icon-only) | All nav links have visible text "Khóa học của tôi" / "Khám phá khóa học" |
| Progress bar | `aria-label` with percentage | `aria-label={Tiến độ hoàn thành: ${progress}%}` — preserve on all Progress instances |
| Lock icons in preview | `aria-hidden="true"` | Decorative — screen readers should skip |
| Filter selects | `aria-label` per Select | `aria-label="Lọc theo lớp"`, `aria-label="Lọc theo khóa học"`, etc. |
| Student name input | Associated `<label>` or `aria-label` | `aria-label="Tìm học sinh theo tên"` |
| Course cards | Keyboard navigable | `<Link>` receives natural focus; no custom tabIndex |
| Enrollment badge | Color not sole indicator | Text label "Đã đăng ký" / "Chưa đăng ký" always present |
| Touch targets | ≥ 48×48px (UX-02) | All buttons: `min-h-[48px]`; nav links: `min-h-[48px]` via header height |
| Color contrast | ≥ 4.5:1 for normal text | `text-muted-foreground` on `bg-background`: #8a7f72 on #fdfaf7 ≈ 3.8:1 — use `text-foreground` for important text |
| 404 page | Role-aware redirect helps users | Done via role detection |

**Contrast note:** `text-muted-foreground` (`hsl(20 10% 45%)`) on `bg-background` (`hsl(30 50% 98%)`) is approximately 4.0:1 — borderline AA for body. Reserve `text-muted-foreground` for truly secondary copy (captions, helper text). All primary content uses `text-foreground`.

---

## Copywriting Contract

All copy is Vietnamese (UX-03). All copy in this contract is the exact string to implement.

### Navigation

| Element | Exact Copy |
|---------|-----------|
| My courses nav link | `Khóa học của tôi` |
| Catalogue nav link | `Khám phá khóa học` |
| Logo alt text | `BuMath` |
| Logo link aria-label | `Trang chủ BuMath` |
| Logout button label | `Đăng xuất` |

### NotFound Page (404)

| Element | Exact Copy |
|---------|-----------|
| Error code | `404` |
| Error message | `Trang không tìm thấy` |
| Action link | `Về trang chủ` |

### Admin Grading Filters

| Element | Exact Copy |
|---------|-----------|
| Grade select placeholder | `Tất cả lớp` |
| Course select placeholder | `Tất cả khóa học` |
| Lesson select placeholder | `Tất cả bài học` |
| Student input placeholder | `Tìm học sinh...` |
| Result count (template) | `Hiển thị {N} / {total} bài nộp` |
| Empty filter result | `Không tìm thấy bài nộp nào phù hợp với bộ lọc.` |
| Grade filter aria-label | `Lọc theo lớp` |
| Course filter aria-label | `Lọc theo khóa học` |
| Lesson filter aria-label | `Lọc theo bài học` |
| Student filter aria-label | `Tìm học sinh theo tên` |

### Course Catalogue Page

| Element | Exact Copy |
|---------|-----------|
| Page heading | `Khám phá khóa học` |
| Page sub-heading | `Tất cả các khóa học đang có tại BuMath` |
| Enrolled badge | `Đã đăng ký` |
| Not enrolled badge | `Chưa đăng ký` |
| Empty state heading | `Chưa có khóa học nào` |
| Empty state body | `Hiện tại chưa có khóa học nào. Vui lòng quay lại sau.` |
| Error state | `Không thể tải danh sách khóa học. Vui lòng làm mới trang.` |

### Course Preview Mode

| Element | Exact Copy |
|---------|-----------|
| Lock notice | `Bạn chưa đăng ký khóa học này.` |
| Contact CTA | `Vui lòng liên hệ giảng viên để được đăng ký khóa học này.` |

### CoursesPage (updated empty state)

| Element | Current | Required |
|---------|---------|---------|
| Empty state heading | `Chưa có khóa học nào` | `Chưa có khóa học nào` *(unchanged)* |
| Empty state body | `Bạn chưa được gán vào khóa học nào. Vui lòng liên hệ giảng viên để được thêm vào khóa học.` | Add: `<Link to="/catalogue" className="text-primary underline mt-3 block">Khám phá tất cả khóa học →</Link>` |

---

## File Change Map

| File | Change Type | Scope |
|------|-------------|-------|
| `src/components/student/StudentLayout.tsx` | Modify | Logo → Link+img, add nav links |
| `src/pages/NotFound.tsx` | Modify | Vietnamese copy, React Router Link, role-aware redirect |
| `src/pages/student/CoursesPage.tsx` | Modify | Progress `className` + empty state catalogue link |
| `src/components/student/LessonSidebar.tsx` | Modify | Progress `className` |
| `src/pages/admin/SubmissionsPage.tsx` | Modify | Filter bar UI (4 controls) + result count + filtered empty state |
| `src/lib/api/submissions.ts` | Modify | Extend `UngradedSubmission` type + `getUngraded` select query |
| `src/lib/api/courses.ts` | Modify | Add `fetchAllCourses()` function |
| `src/pages/student/CataloguePage.tsx` | Create | New course catalogue page |
| `src/pages/student/CourseDetailPage.tsx` | Modify | Add enrollment query + preview mode rendering branch |
| `src/App.tsx` | Modify | Add `/catalogue` route |
| `supabase/migrations/20260428_13_catalogue_rls.sql` | Create | Broaden courses/chapters/lessons SELECT RLS policies |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Select, Input, Badge, Card, Progress, Skeleton, Alert, Table, Button (all already installed) | Not required — official registry |
| Third-party | None | Not applicable |

No new packages. No third-party registries. No registry vetting required.

---

## Design Decisions Log

| Decision | Rationale | Source |
|----------|-----------|--------|
| Progress track → `bg-muted` (not `bg-secondary`) | `--secondary` is cyan/blue — visually competes with primary orange. `--muted` is warm neutral, recedes correctly | RESEARCH.md §4 |
| Logo uses `BASE_URL` env pattern | Matches existing `Header.tsx` in landing page | RESEARCH.md §3 |
| Nav links hidden on mobile (`hidden sm:flex`) | 48px header cannot accommodate horizontal nav + logout on 375px | RESEARCH.md §3 |
| CataloguePage cards omit Progress bar | Non-enrolled students have no progress — showing 0% is misleading | UX reasoning |
| Enrollment badge: green for enrolled | Green = positive/affirming state — consistent with submission "Đã nộp" badge convention | Color semantics |
| Enrollment query fail → preview mode | Fail-safe: never grant access accidentally. Preview is the conservative fallback | Security |
| No "clear filters" button | Each Select reverts to placeholder = filter cleared. Input cleared by user typing. Simplest UX for low filter count | UX |
| GRADE_BADGE reused for catalogue | Existing constant maps `target_grade` to labels and Tailwind classes — single source of truth | RESEARCH.md §1 |
| Vietnamese throughout | UX-03 requirement — all UI text must be Vietnamese | REQUIREMENTS.md |
| Weight 500 (Medium) removed | Reduces on-screen weight count from 4 to 2 (400+600). All formerly "medium" interactive elements (nav links, buttons, badges, form labels) use 600 Semibold. 700 Bold retained as undeclared single-element exception for 404 number only. | gsd-ui-checker Issue 1 |
| Nav link `py-1.5` → `py-2` | 6px is off the 4-point grid. 8px (`py-2`) keeps padding grid-compliant. Touch target is enforced by header `min-h-[48px]` + flex centering, not by link padding. | gsd-ui-checker Issue 2 |
| Filter bar `gap-3` → `gap-2` | 12px is not in the standard spacing set {4,8,16,24,32,48,64}. 8px gap (`gap-2`) keeps the filter bar compact and grid-compliant. | gsd-ui-checker Issue 3 |
| Focal points added for Screen 5 and Screen 6 | Explicit primary visual anchor declarations per Dimension 2 requirement. | gsd-ui-checker FLAG |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
