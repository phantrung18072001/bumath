---
phase: 20
slug: thay-i-ui-ux-landing-page-hi-n-i-theo-phong-c-ch-ai-edtech-saas
status: draft
shadcn_initialized: true
preset: default / baseColor:slate / cssVariables:true
created: 2026-05-08
---

# Phase 20 — UI Design Contract
## Student + Admin UI/UX: AI EdTech SaaS Design Language

> Visual and interaction contract for the Phase 20 UI-only overhaul.
> All decisions pre-populated from CONTEXT.md (D-01–D-17). No new design decisions were required.
> Generated by gsd-ui-researcher. To be verified by gsd-ui-checker.

---

## Scope Boundary

| In Scope | Out of Scope |
|----------|--------------|
| Student: `CoursesPage`, `CataloguePage`, `CourseDetailPage`, `ProfilePage`, `StudentLayout` | Landing page `/` — do NOT touch |
| Admin: `CoursesPage`, `UsersPage`, `GradingPage`, `SubmissionsPage`, `PackagesPage`, admin layout/sidebar | New features, API changes, route changes |
| Component-level: cards, buttons, badges, progress bars, inputs, navigation active states, LessonSidebar | `src/components/ui/*.tsx` shadcn base files — className overrides ONLY |
| CSS: `src/index.css` custom classes and `:root` CSS variables | Page-level backgrounds (`bg-[#F0FDFA]`, `bg-background`, layout wrappers) |

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | `components.json` detected |
| Style | default | `components.json` |
| Base color | slate | `components.json` |
| CSS variables | true | `components.json` |
| Component library | Radix UI primitives (via shadcn) | codebase |
| Icon library | Lucide React | codebase (`lucide-react` imports throughout) |
| Font | Be Vietnam Pro (400, 500, 600, 700, 800, 900) | `src/index.css` Google Fonts import |
| Motion | Framer Motion — keep existing patterns, do not remove | CONTEXT.md |
| Drag/drop | dnd-kit — keep as-is | codebase |

---

## Color Contract

### New Palette (D-03, D-04 — LOCKED)

| Role | Hex | Tailwind Equivalent | Usage |
|------|-----|---------------------|-------|
| Primary (indigo) | `#4F46E5` | `indigo-600` | Buttons (default), active nav, progress bar, focus rings, active sidebar state, icon accent |
| Secondary (purple) | `#7C3AED` | `violet-600` | Gradient endpoint, CTA gradient, heading gradient, admin role badge (keep existing purple-600) |
| Accent (cyan) | `#06B6D4` | `cyan-500` | Reserved for: data callout numbers, "enrolled" status indicator only |
| Text dominant | `#0F172A` | `slate-900` | All headings, card titles, primary body text |
| Text muted | `#64748B` | `slate-500` | Descriptions, secondary labels, counts |
| Destructive | `#EF4444` | `red-500` | Destructive button/action only — no change from current |
| Success | `#22C55E` | `green-500` | "Đã đăng ký" (enrolled), graded status — keep current green badges |

> **60 / 30 / 10 split:**
> - 60% Dominant: page backgrounds (LOCKED — not modified), card surfaces (`bg-white/80`)
> - 30% Secondary: sidebar, table header rows, section labels, muted areas
> - 10% Accent: indigo on interactive elements; cyan **only** on enrolled badge and stat numbers

### Background Consistency Lock (D-05, D-06 — CRITICAL)

```
DO NOT modify any of these in any file:
  bg-[#F0FDFA]          ← student page background (if present)
  bg-background          ← CSS var background
  bg-white               ← StudentLayout wrapper (h-screen)
  Any layout wrapper background class
```

Only these surfaces change: cards, buttons, badges, progress bars, inputs, nav active states.

### CSS Variable Updates Required in `src/index.css`

Update `:root` block — these are the ONLY variable changes needed:

```css
/* Replace orange primary → indigo */
--primary: 245 94% 58%;           /* was: 24 95% 53% — #4F46E5 */
--primary-foreground: 0 0% 100%;  /* stays white */
--ring: 245 94% 58%;              /* was: 24 95% 53% — focus rings now indigo */

/* Update sidebar to match */
--sidebar-primary: 245 94% 58%;
--sidebar-ring: 245 94% 58%;

/* Update BuMath custom vars */
--bm-primary: #4F46E5;
--bm-cta: #4F46E5;
--bm-border: #4F46E5;
```

> These variable changes automatically propagate to ALL `text-primary`, `bg-primary`, `ring-ring`, `border-primary`, `border-l-2 border-primary` usages across all screens — no manual className hunt needed for those patterns.

---

## Spacing Scale

Declared values (all multiples of 4px — no exceptions for this phase):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon internal gaps (`gap-1`), tight inline padding |
| sm | 8px | Badge padding, compact element spacing (`gap-2`) |
| md | 16px | Card internal padding (`p-4`), form field gaps |
| lg | 24px | Section padding (`p-6`), card header/content separation |
| xl | 32px | Page inner padding (`p-8`) |
| 2xl | 48px | Page vertical padding (`py-12`) |
| 3xl | 64px | Major section breaks, hero heights |

Exception: touch targets — minimum `min-h-[48px]` on all interactive elements (existing pattern — keep).
Sidebar hover zones — minimum `min-h-[48px]` per lesson item (existing — keep).

---

## Typography

| Role | Size | Weight | Line Height | Color | Tailwind |
|------|------|--------|-------------|-------|---------|
| Display heading | 28px / `text-2xl` | 700 bold | 1.2 (`leading-tight`) | `#0F172A` | `text-2xl font-bold tracking-tight text-slate-900` |
| Section heading | 20px / `text-xl` | 700 bold | 1.2 | `#0F172A` | `text-xl font-bold tracking-tight text-slate-900` |
| Card title | 16px / `text-base` | 700 bold | 1.4 (`leading-snug`) | `#0F172A` | `text-base font-bold leading-snug text-slate-900` |
| Body / label | 14px / `text-sm` | 400 regular | 1.5 (`leading-relaxed`) | `#64748B` | `text-sm text-slate-500 leading-relaxed` |

> Gradient text application (D-11): Apply `bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent` on **page-level h1 headings** only (CoursesPage h1, CataloguePage h1, Admin section headings). Do NOT apply to card titles or body text.

> Font weight rule (D-10): Only 2 weights in use — 400 (body, labels, descriptions, inactive nav items) and 700 (headings, card titles, button labels, active nav items). No other weights.

> `text-[#92400E]` (brown) found in existing code — **replace all instances** with `text-[#0F172A]` or gradient variant per the table above.

---

## Card Component Contract

### New Glassmorphism Card (D-02, D-07, D-08)

**Replaces:** `.bm-clay-card-student` (orange-bordered, double shadow)

**New CSS class to add in `src/index.css`:**

```css
/* === BuMath Glassmorphism Card (Phase 20) === */
.bm-glass-card {
  background: rgba(255, 255, 255, 0.80);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border: 1px solid rgba(255, 255, 255, 0.30);
  border-radius: 24px;
  box-shadow:
    0 10px 15px rgba(0, 0, 0, 0.08),
    0 4px 6px rgba(0, 0, 0, 0.04);
  transition: box-shadow 200ms ease, transform 200ms ease;
}

.bm-glass-card:hover {
  box-shadow:
    0 20px 25px rgba(0, 0, 0, 0.12),
    0 8px 10px rgba(0, 0, 0, 0.06);
  transform: translateY(-4px);
}

@media (prefers-reduced-motion: reduce) {
  .bm-glass-card,
  .bm-glass-card:hover {
    transition: none;
    transform: none;
  }
}
```

**Usage pattern** (className override on shadcn `Card`):
```tsx
<Card className="bm-glass-card border-0 shadow-none p-0 overflow-hidden">
```

**Where to apply:**
- `src/pages/student/CoursesPage.tsx` — course enrollment cards
- `src/pages/student/CataloguePage.tsx` — catalogue course cards
- `src/pages/student/ProfilePage.tsx` — profile card, package cards (both the `Card` component and the bare `div.bm-clay-card-student`)
- `src/pages/admin/CoursesPage.tsx` — wrap table in glassmorphism panel
- `src/pages/admin/UsersPage.tsx` — wrap table in glassmorphism panel
- `src/pages/admin/GradingPage.tsx` — content panels
- `src/pages/admin/SubmissionsPage.tsx` — wrap table in glassmorphism panel
- `src/pages/admin/PackagesPage.tsx` — wrap table in glassmorphism panel

**Keep old `.bm-clay-card`** (used in landing page — out of scope). Only `.bm-clay-card-student` is replaced.

### Admin Table Panel Pattern

Admin pages currently have bare `<div className="container mx-auto px-4 py-8">` wrappers. Apply a glassmorphism container around the table:

```tsx
<div className="bm-glass-card p-6">
  {/* Table here */}
</div>
```

---

## Progress Bar Contract

**New CSS class to add in `src/index.css`:**

```css
/* === BuMath Indigo Progress Bar (Phase 20 — replaces bm-progress-teal) === */
.bm-progress-indigo [data-slot="progress-indicator"],
.bm-progress-indigo > div {
  background-color: #4F46E5 !important;
}
```

**Usage:**
```tsx
<Progress
  value={progress}
  className="h-1.5 bg-indigo-100 bm-progress-indigo"
  aria-label={`Tiến độ hoàn thành: ${progress}%`}
/>
```

Replace `bg-[#FFEDD5] bm-progress-teal` → `bg-indigo-100 bm-progress-indigo` in:
- `src/pages/student/CoursesPage.tsx` (course card progress)
- `src/components/student/LessonSidebar.tsx` (sidebar progress)

---

## Button Contract (D-12)

### Primary Button (CTA / Default)
```tsx
// Standard primary action
<Button className="bg-indigo-600 hover:bg-indigo-700 text-white min-h-[48px] cursor-pointer">
  Label
</Button>

// Gradient CTA (main page-level CTAs)
<Button className="bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 text-white min-h-[48px] cursor-pointer transition-all duration-200">
  Label
</Button>
```

### Update `.bm-btn-cta` in `src/index.css`:
```css
.bm-btn-cta {
  background: linear-gradient(to right, #4F46E5, #7C3AED) !important;
  color: #ffffff;
  border: none;
  border-radius: 8px;
  font-weight: 700;
  min-height: 48px;
  cursor: pointer;
  transition: opacity 200ms ease, transform 200ms ease;
}
.bm-btn-cta:hover:not(:disabled) {
  opacity: 0.9;
  transform: translateY(-1px);
}
.bm-btn-cta:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  transform: none;
}
```

### Outline / Ghost buttons
- Keep `variant="outline"` and `variant="ghost"` from shadcn — `--ring` update handles focus
- Destructive: `variant="outline"` with `text-destructive border-destructive hover:bg-destructive hover:text-destructive-foreground` — KEEP AS-IS

### Icon-only action buttons (admin tables)
- Keep `min-h-[48px]` touch target
- Focus ring: updated automatically via `--ring` CSS var → indigo

---

## Badge Contract (D-13)

### Replace orange/teal badge instances

| Old pattern | New pattern | Usage |
|-------------|-------------|-------|
| Any `bg-[#F97316]` / `text-[#F97316]` badge | `bg-indigo-100 text-indigo-700 hover:bg-indigo-100` | General indigo badge |
| Enrolled status badge | `bg-cyan-100 text-cyan-700 hover:bg-cyan-100` | "Đã đăng ký" — accent color D-03 |
| Not enrolled badge | Keep `variant="outline" text-muted-foreground` | "Chưa đăng ký" |
| "Công khai" (published) | Keep `bg-green-100 text-green-700` | semantic — do not change |
| "Nháp" (draft) | Keep `variant="outline" text-muted-foreground` | semantic — do not change |
| Admin role badge | Keep `bg-purple-600 text-white` | semantic — do not change |
| Teacher role badge | Keep `bg-blue-600 text-white` | semantic — do not change |
| Student role badge | Keep `variant="secondary"` | semantic — do not change |

**Grade badges** — KEEP ALL AS-IS (semantic color coding):
- Lớp 7: `bg-blue-100 text-blue-700`
- Lớp 8: `bg-green-100 text-green-700`
- Lớp 9: `bg-purple-100 text-purple-700`
- Ôn chuyên: `bg-orange-100 text-orange-700`

---

## Navigation Contract

### StudentLayout Header (D-12, D-14)

Current active NavLink class: `text-primary` (orange via CSS var)
After CSS var update: `text-primary` → indigo automatically

Logo span `text-primary` → also indigo automatically.

No className changes needed in `StudentLayout.tsx` for active states — CSS var propagation handles it.

Hover state for non-active NavLink: `hover:bg-muted` — keep as-is.

### LessonSidebar (D-14)

Replace orange-specific hardcoded classes:

| Current | New |
|---------|-----|
| `border-[#F97316]/15` (chapter border) | `border-indigo-200/60` |
| `border-[#F97316]/20` (progress section border) | `border-indigo-200` |
| `bg-[#FFEDD5]` (progress bg) | `bg-indigo-100` |
| `border-[#F97316]/40 text-[#92400E]/50 hover:text-[#92400E] hover:border-[#F97316]/70 hover:bg-[#FFF7ED]` (add lesson btn) | `border-indigo-300/50 text-indigo-400 hover:text-indigo-700 hover:border-indigo-400 hover:bg-indigo-50` |
| `border-[#F97316]/40 text-[#92400E] hover:bg-[#FFEDD5]/50` (add chapter btn) | `border-indigo-300/60 text-indigo-700 hover:bg-indigo-50/50` |
| `border-[#F97316]/20` (chapter footer border) | `border-indigo-200/60` |
| `bg-sidebar-accent border-l-2 border-primary` (active lesson) | Updated automatically via `--primary` var |
| `text-primary` (active ChevronRight icon) | Updated automatically via `--primary` var |

---

## Input / Form Contract (D-14)

### Search Input (CataloguePage, Admin pages)

Current: `border-[#F97316] focus-visible:ring-[#F97316]`
New: Remove explicit orange overrides — `focus-visible:ring-indigo-500` is applied automatically via `--ring` update.

For CataloguePage search input:
```tsx
<Input
  className="pl-9 min-h-[48px] rounded-xl focus-visible:ring-indigo-500"
  ...
/>
```

Remove `border-[#F97316]` from CataloguePage search input className.

### Grade Filter Pills (CataloguePage)

```tsx
// Active pill
'bg-indigo-600 text-white border-indigo-600'
// Inactive pill
'bg-background text-muted-foreground border-border hover:bg-muted hover:text-foreground'
```

---

## Icon Container Pattern (D-11 specifics)

For admin dashboard stat icons and any icon wrapper divs:

```tsx
<div className="w-10 h-10 rounded-xl bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center">
  <Icon className="h-5 w-5 text-white" />
</div>
```

Used in: Admin CoursesPage header, UsersPage header, PackagesPage header (upgrade the plain `<Plus>` icon area to a branded icon container).

---

## Empty State Contract

All empty state icons: change from `text-[#F97316]` → `text-indigo-400`.

| Screen | Heading | Body | CTA |
|--------|---------|------|-----|
| Student CoursesPage (no enrollments) | "Bạn chưa có khóa học nào" | "Liên hệ giảng viên để được thêm vào khóa học, hoặc khám phá danh mục." | "Khám phá khóa học" → gradient CTA button |
| Student CataloguePage (search 0 results) | "Không tìm thấy kết quả" | "Thử thay đổi từ khóa hoặc chọn lớp khác." | no CTA |
| Student CataloguePage (no courses at all) | "Chưa có khóa học nào" | "Hiện tại chưa có khóa học nào. Vui lòng quay lại sau." | no CTA |
| Student ProfilePage (no packages) | "Bạn chưa có gói học nào" | "Liên hệ giảng viên để được gán gói học phù hợp với lớp của bạn." | no CTA |
| Admin CoursesPage (empty, no filter) | "Chưa có khóa học nào" | `Nhấn "Tạo khóa học" để bắt đầu.` | "Tạo khóa học" |
| Admin CoursesPage (filtered 0) | "Không tìm thấy kết quả" | "Thử thay đổi từ khóa hoặc bộ lọc." | no CTA |

> Copy is unchanged from current — only the icon color and container card style change.

---

## Error State Contract

| Screen | Copy |
|--------|------|
| Student CoursesPage | "Không thể tải dữ liệu. Vui lòng thử lại." |
| Student CataloguePage | "Không thể tải dữ liệu. Vui lòng thử lại." |
| Admin grading error | `text-destructive` — keep existing pattern |

Style: `text-destructive text-center py-8` — keep exactly as-is across all screens.

---

## Copywriting Contract

| Element | Copy | Notes |
|---------|------|-------|
| Primary CTA (student, no enrollments) | "Khám phá khóa học" | Keep; change button class only |
| Primary CTA (catalogue, unauthenticated) | "Đăng nhập để học" | Keep; change button class only |
| Primary CTA (admin courses) | "Tạo khóa học" | Keep; upgrade to indigo gradient button |
| Primary CTA (admin packages) | "Tạo gói học" | Keep; upgrade to indigo gradient button |
| Delete confirmation (admin course) | "Xóa khóa học" / "Bạn có chắc muốn xóa khóa học [name]? Toàn bộ chuyên đề và bài học trong khóa sẽ bị xóa theo. Hành động này không thể hoàn tác." | Keep exact copy |
| Delete confirmation (admin chapter) | "Xóa chuyên đề" / destructive pattern | Keep exact copy |
| Delete confirmation (admin lesson) | "Xóa bài giảng" / destructive pattern | Keep exact copy |
| Delete confirmation (admin package) | "Xóa gói học" / destructive pattern | Keep exact copy |

> Destructive confirmation approach: use existing `AlertDialog` pattern with `AlertDialogAction className="bg-destructive text-destructive-foreground hover:bg-destructive/90"` — UNCHANGED.

---

## Gradient Text Application (D-11)

Apply `bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent` to:

| Location | Element | Current |
|----------|---------|---------|
| `CoursesPage` | `<h1>Khóa học của tôi</h1>` | `text-[#92400E]` |
| `CataloguePage` | `<h1>Khám phá khóa học</h1>` | `text-[#92400E]` |
| `ProfilePage` | Profile hero greeting text optional | `text-white` (keep white on dark hero) |
| Admin `CoursesPage` | `<h1>Quản lý khóa học</h1>` | `text-xl font-bold` |
| Admin `UsersPage` | `<h1>Quản lý người dùng</h1>` | plain text |
| Admin `PackagesPage` | `<h1>Quản lý gói học</h1>` | plain text |

> Do NOT apply gradient to: card titles, description text, badge labels, table cell text.

---

## ProfilePage Hero Gradient Update

Current: `from-primary/85 via-primary/60 to-indigo-500/50`
New (explicit, not relying on --primary):
```tsx
className="absolute inset-0 bg-gradient-to-r from-indigo-600/85 via-indigo-500/60 to-purple-500/50"
```

Avatar container: `bg-primary/10` → `bg-indigo-50`, `text-primary` → `text-indigo-600`
Stats container: `bg-primary/5` → `bg-indigo-50/50`, `text-primary` → `text-indigo-600`
Study illustration overlay: `from-primary/60` → `from-indigo-600/60`

---

## Responsive Breakpoints

All changes must hold at:

| Breakpoint | Width | Key behavior |
|------------|-------|--------------|
| Mobile | 375px | Cards full-width, 1-col grid, filter pills wrap |
| Tablet | 768px | 2-col card grid, search at 50% width |
| Laptop | 1024px | 4-col card grid, sidebar visible |
| Desktop | 1440px | Same as 1024, container max-width capped |

> No responsive breakpoint changes. Only card class and color class swaps.

---

## Accessibility Contract

- Contrast minimum: 4.5:1 for all text. `#4F46E5` on white = 4.65:1 ✓ (passes AA)
- `#0F172A` on white = 19.1:1 ✓ (passes AAA)
- `#64748B` on white = 4.6:1 ✓ (passes AA)
- Cyan `#06B6D4` on white = 2.5:1 — use only for decorative icons, NOT for text
- Focus rings: `--ring` updated to indigo → all shadcn interactive elements get `ring-indigo-600` automatically
- `cursor-pointer` — existing pattern, keep on ALL clickable elements
- `min-h-[48px]` — existing pattern, keep on ALL interactive elements
- `aria-label` — keep all existing aria labels. Icon-only action buttons in admin tables MUST have explicit aria-label values: `aria-label="Chỉnh sửa"` (edit), `aria-label="Xóa"` (delete). Executors must verify these exist on every icon button during the style refactor.
- `prefers-reduced-motion` — add to `.bm-glass-card` in CSS (see card contract above)
- Screen reader: no changes to semantic HTML structure

---

## Animation Contract

| Element | Effect | Duration |
|---------|--------|----------|
| Card hover | `translateY(-4px)` + shadow increase | 200ms ease |
| Button hover | `translateY(-1px)` + opacity | 200ms ease |
| Transitions (all) | `transition-all duration-200` or `transition-colors duration-150` | 150–200ms |
| Framer Motion | Keep existing `initial/animate/whileInView` patterns | no change |
| Skeleton loading | Keep existing `animate-pulse` | no change |
| Accordion | Keep `accordion-down/up` keyframes | no change |

---

## Screen-by-Screen Change Summary

### Student Screens

| Screen | Changes Required |
|--------|-----------------|
| `StudentLayout.tsx` | CSS var update propagates `text-primary` active states to indigo automatically. No file changes needed unless orange hardcoded values found. |
| `CoursesPage.tsx` | h1: gradient text. Cards: `bm-clay-card-student` → `bm-glass-card`. Progress: `bm-progress-teal` → `bm-progress-indigo`, `bg-[#FFEDD5]` → `bg-indigo-100`. Empty icon: `text-[#F97316]` → `text-indigo-400`. CTA button: `bm-btn-cta` (inherits gradient after CSS update). Heading color: `text-[#92400E]` → gradient. |
| `CataloguePage.tsx` | h1: gradient text. Cards: glassmorphism. Search input: remove `border-[#F97316]` override. Filter pills: `bg-[#F97316]` → `bg-indigo-600`. Empty icons: `text-[#F97316]` → `text-indigo-400`. Heading colors: `text-[#92400E]` → `text-[#0F172A]`. CTA btn: update class. |
| `CourseDetailPage.tsx` | Course title area: gradient text on course heading. Card/panel: glassmorphism. Badge and button colors: via CSS var propagation. Locked content indication: indigo lock icon. |
| `ProfilePage.tsx` | Hero gradient: explicit indigo colors (see hero section above). Cards: glassmorphism. Avatar/stats: indigo palette. Package cards: `bm-clay-card-student` div → `bm-glass-card`. |
| `LessonSidebar.tsx` | Progress section: indigo variants. Chapter/lesson borders: indigo variants. Add chapter/lesson buttons: indigo dashed style. Active state: CSS var propagation. |

### Admin Screens

| Screen | Changes Required |
|--------|-----------------|
| `admin/CoursesPage.tsx` | h1: gradient text. Table: wrap in `bm-glass-card`. Primary "Tạo khóa học" button: gradient CTA. Action buttons: CSS var propagation. Grade badges: KEEP. Published/draft badges: KEEP. |
| `admin/UsersPage.tsx` | h1: gradient text. Table: wrap in `bm-glass-card`. Role badges (admin/teacher/student): KEEP. Grade badges: KEEP. |
| `admin/GradingPage.tsx` | Panel containers: glassmorphism. Action buttons: CSS var propagation. Grading form cards: glassmorphism. |
| `admin/SubmissionsPage.tsx` | h1: gradient text. Table: wrap in `bm-glass-card`. Filter toolbar: indigo active pill. |
| `admin/PackagesPage.tsx` | h1: gradient text. Table: wrap in `bm-glass-card`. "Tạo gói học" button: gradient CTA. Grade badges in packages: KEEP. |

---

## CSS Changes Summary

The following changes to `src/index.css` are the complete set:

1. **Update `:root` CSS variables** — `--primary`, `--ring`, `--sidebar-primary`, `--sidebar-ring`, `--bm-primary`, `--bm-cta`, `--bm-border` to indigo values
2. **Replace `.bm-clay-card-student`** — keep the selector, replace the content with glassmorphism properties (OR keep old + add new `.bm-glass-card`; prefer keeping old for any edge case backward compat and using `bm-glass-card` in JSX)
3. **Update `.bm-btn-cta`** — gradient `from #4F46E5 to #7C3AED`
4. **Add `.bm-progress-indigo`** — indigo fill override
5. **Add `.bm-glass-card`** — new glassmorphism card class with reduced-motion support
6. **Keep `.bm-clay-card`** — NOT changed (used in landing, out of scope)
7. **Keep `.bm-float-symbol-light`** — float symbols div is `hidden` class in StudentLayout; no visible impact

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | All components already installed (Card, Badge, Button, Progress, Table, Badge, Input, Select, Pagination, AlertDialog, etc.) | not required — no new installs in this phase |
| Third-party | none | not applicable |

> This phase requires **zero new shadcn component installs** — all needed components are already in `src/components/ui/`. Implementation is className overrides + CSS class additions only.

---

## Audit Process (D-15, D-16)

Per CONTEXT.md decisions, the plan must sequence:

1. **Student audit pass** — use `ui-ux-pro-max` skill on all 5 student screens + LessonSidebar
2. **Student implementation** — apply findings from audit
3. **Admin audit pass** — use `ui-ux-pro-max` skill on all 5 admin screens
4. **Admin implementation** — apply findings from admin audit

Do NOT bundle student + admin into one task. Separate audit passes produce separate fix lists.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS (FLAG: aria-label note added for icon-only buttons)
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS (fixed — semibold weight removed, nav uses 400/700 only)
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-05-08

---

*Phase: 20-thay-i-ui-ux-landing-page-hi-n-i-theo-phong-c-ch-ai-edtech-saas*
*UI-SPEC generated: 2026-05-08*
*All decisions sourced from CONTEXT.md — no user re-questioning required*
