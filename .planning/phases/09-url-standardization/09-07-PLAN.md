---
phase: 09-url-standardization
plan: 07
type: execute
wave: 3
depends_on: [09-02, 09-03, 09-04, 09-05, 09-06]
files_modified:
  - src/pages/Login.test.tsx
  - src/pages/NotFound.test.tsx
  - src/components/admin/AdminLayout.test.tsx
  - src/components/auth/ProtectedRoute.test.tsx
  - src/components/student/StudentLayout.test.tsx
  - src/pages/student/CourseDetailPage.test.tsx
autonomous: true
requirements: [URL-01, URL-02, URL-03]

must_haves:
  truths:
    - "yarn test passes with zero failures after all URL renames"
    - "All test assertions check Vietnamese URL strings, not English ones"
  artifacts:
    - path: "src/pages/Login.test.tsx"
      provides: "Login page navigate assertions"
      contains: "/khoa-hoc, /quan-tri/nguoi-dung, /quan-tri/bai-nop"
    - path: "src/pages/NotFound.test.tsx"
      provides: "NotFound homeLink assertion"
      contains: "/khoa-hoc"
    - path: "src/components/admin/AdminLayout.test.tsx"
      provides: "AdminLayout nav href assertions"
      contains: "/quan-tri/nguoi-dung, /quan-tri/khoa-hoc, /quan-tri/bai-nop"
    - path: "src/components/auth/ProtectedRoute.test.tsx"
      provides: "ProtectedRoute redirect assertions"
      contains: "/khoa-hoc, /quan-tri/bai-nop, /dang-nhap"
    - path: "src/components/student/StudentLayout.test.tsx"
      provides: "StudentLayout nav href assertions"
      contains: "/khoa-hoc, /danh-muc, /quan-tri/nguoi-dung"
    - path: "src/pages/student/CourseDetailPage.test.tsx"
      provides: "CourseDetailPage route and URL assertions"
      contains: "/khoa-hoc/:courseSlug"
  key_links:
    - from: "test files"
      to: "source implementations"
      via: "URL string assertions"
      pattern: "expect.*quan-tri|expect.*khoa-hoc|expect.*dang-nhap"
---

<objective>
Update all 6 test files to match the Vietnamese URLs now used in source code. Tests were written against English URLs — they will fail until updated.

Purpose: Tests are the safety net for Phase 9's changes. After all source files are updated in Waves 1-2, this plan makes the test suite green again. Running `yarn test` after this plan should pass completely.

Output: 6 test files updated with Vietnamese URL assertions; `yarn test` passes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-url-standardization/09-CONTEXT.md
@.planning/phases/09-url-standardization/09-RESEARCH.md
</context>

<interfaces>
<!-- Complete URL mapping for test assertions -->
/login               → /dang-nhap
/register            → /dang-ky
/courses             → /khoa-hoc
/courses/:courseSlug → /khoa-hoc/:courseSlug
/catalogue           → /danh-muc
/admin/users         → /quan-tri/nguoi-dung
/admin/courses       → /quan-tri/khoa-hoc
/admin/submissions   → /quan-tri/bai-nop
/admin/submissions/:id → /quan-tri/bai-nop/:id
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Login, NotFound, and AdminLayout test files</name>
  <files>
    src/pages/Login.test.tsx,
    src/pages/NotFound.test.tsx,
    src/components/admin/AdminLayout.test.tsx
  </files>
  <action>
**Login.test.tsx** — 3 navigate assertion updates:
- `expect(navigate).toHaveBeenCalledWith('/courses')` → `'/khoa-hoc'`
- `expect(navigate).toHaveBeenCalledWith('/admin/users')` → `'/quan-tri/nguoi-dung'`
- `expect(navigate).toHaveBeenCalledWith('/admin/submissions')` → `'/quan-tri/bai-nop'`

Also update any test description strings that mention old URLs (e.g., `it('navigates to /courses for student')`).

**NotFound.test.tsx** — 1 href assertion update:
- Any `expect(...).toHaveAttribute('href', '/courses')` → `'/khoa-hoc'`
- Also update test description strings if they mention `/courses`.

**AdminLayout.test.tsx** — 3 href assertion updates:
- `/admin/users` → `/quan-tri/nguoi-dung`
- `/admin/courses` → `/quan-tri/khoa-hoc`
- `/admin/submissions` → `/quan-tri/bai-nop`

Also update any test description strings that mention old admin URLs.

Do NOT change test logic, mock structures, or what the tests are actually verifying — only the URL string values in assertions and descriptions.
  </action>
  <verify>
    <automated>grep -n "'/courses\|'/admin/users\|'/admin/courses\|'/admin/submissions\|href.*courses\|href.*admin" src/pages/Login.test.tsx src/pages/NotFound.test.tsx src/components/admin/AdminLayout.test.tsx</automated>
  </verify>
  <done>Zero lines returned — no English URL strings remain in these three test files.</done>
</task>

<task type="auto">
  <name>Task 2: Update ProtectedRoute, StudentLayout, and CourseDetailPage test files</name>
  <files>
    src/components/auth/ProtectedRoute.test.tsx,
    src/components/student/StudentLayout.test.tsx,
    src/pages/student/CourseDetailPage.test.tsx
  </files>
  <action>
**ProtectedRoute.test.tsx** — multiple assertion and description updates:
- Any redirect assertion to `/courses` → `/khoa-hoc`
- Any redirect assertion to `/admin/submissions` → `/quan-tri/bai-nop`
- Any `<Navigate to="/login">` test → check for `/dang-nhap`
- Test description strings mentioning `/login`, `/courses`, `/admin/submissions` → update to Vietnamese
- Any `initialEntries` in MemoryRouter mocking old protected route URLs → update if present

**StudentLayout.test.tsx** — 3 href assertion updates:
- `/courses` → `/khoa-hoc`
- `/catalogue` → `/danh-muc`
- `/admin/users` → `/quan-tri/nguoi-dung`

Also update test description strings mentioning old URLs.

**CourseDetailPage.test.tsx** — route path and URL updates:
- MemoryRouter `initialEntries` and `path` props: `/courses/:courseSlug` → `/khoa-hoc/:courseSlug`
- Any `window.history.pushState` URL references: update `/courses/...` → `/khoa-hoc/...`
- Any assertions checking for `/login` link → `/dang-nhap`
- Test description strings mentioning old URLs

After updating all 6 test files, run the full test suite to confirm everything passes.
  </action>
  <verify>
    <automated>yarn test --run 2>&1 | tail -20</automated>
  </verify>
  <done>`yarn test` exits with code 0. Zero test failures. All 6 test files assert Vietnamese URL strings.</done>
</task>

</tasks>

<verification>
```bash
# Scan for any remaining English URL strings in ALL test files
grep -rn "'/login\|'/register\|'/courses\|'/catalogue\|'/admin/users\|'/admin/courses\|'/admin/submissions\|/courses/:courseSlug\|href.*'/courses\|href.*'/admin" \
  src/ --include="*.test.*"
# Must return 0 lines

# Run full test suite
yarn test --run
# Must exit 0, all tests green
```
</verification>

<success_criteria>
- All 6 test files updated: zero English URL strings in assertions or test descriptions
- `yarn test --run` passes with 0 failures
- No test logic or mock structure changed — only URL string values updated
</success_criteria>

<output>
After completion, create `.planning/phases/09-url-standardization/09-07-SUMMARY.md`
</output>
