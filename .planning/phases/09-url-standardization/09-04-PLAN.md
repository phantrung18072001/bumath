---
phase: 09-url-standardization
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/pages/admin/CoursesPage.tsx
  - src/pages/admin/ChaptersPage.tsx
  - src/pages/admin/LessonsPage.tsx
autonomous: true
requirements: [URL-01, URL-02, URL-03]

must_haves:
  truths:
    - "CoursesPage row-click navigates to /quan-tri/khoa-hoc/:slug"
    - "ChaptersPage chapter row-click navigates to /quan-tri/khoa-hoc/:courseSlug/chuong/:chapterSlug"
    - "ChaptersPage and LessonsPage breadcrumbs link to correct Vietnamese ancestor paths"
  artifacts:
    - path: "src/pages/admin/CoursesPage.tsx"
      provides: "Course list page with row-click navigation"
      contains: "/quan-tri/khoa-hoc/, /quan-tri/bai-nop"
    - path: "src/pages/admin/ChaptersPage.tsx"
      provides: "Chapter list page with navigation and breadcrumbs"
      contains: "/quan-tri/khoa-hoc, chuong/"
    - path: "src/pages/admin/LessonsPage.tsx"
      provides: "Lesson list page with breadcrumbs"
      contains: "/quan-tri/khoa-hoc"
  key_links:
    - from: "src/pages/admin/ChaptersPage.tsx"
      to: "/quan-tri/khoa-hoc/:courseSlug/chuong/:chapterSlug"
      via: "navigate() call"
      pattern: "navigate.*quan-tri/khoa-hoc.*chuong"
---

<objective>
Update all URL strings in the three admin content-management pages: CoursesPage (navigate to course detail + link to submissions), ChaptersPage (navigate to chapter detail + breadcrumb back to courses), and LessonsPage (breadcrumb links).

Purpose: These pages form the admin content-management flow. Broken navigation here would trap admins/teachers on list pages with no way to drill into course or chapter detail.

Output: CoursesPage.tsx (2 changes), ChaptersPage.tsx (2 changes), LessonsPage.tsx (2 changes).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-url-standardization/09-CONTEXT.md
@.planning/phases/09-url-standardization/09-RESEARCH.md
</context>

<interfaces>
<!-- Route paths now defined in App.tsx (after Plan 01) -->
/quan-tri/khoa-hoc                                              → was /admin/courses
/quan-tri/khoa-hoc/:courseSlug                                  → was /admin/courses/:courseSlug
/quan-tri/khoa-hoc/:courseSlug/chuong/:chapterSlug              → was /admin/courses/:courseSlug/chapters/:chapterSlug
/quan-tri/bai-nop                                               → was /admin/submissions

<!-- NOTE: The dynamic param segment "chapters" is renamed to "chuong" -->
<!-- i.e., /admin/courses/my-course/chapters/ch-1 → /quan-tri/khoa-hoc/my-course/chuong/ch-1 -->
<!-- :chapterSlug param name stays the same — only the static "chapters" segment changes to "chuong" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update CoursesPage.tsx navigate calls</name>
  <files>src/pages/admin/CoursesPage.tsx</files>
  <action>
**CoursesPage.tsx** — 2 changes (per URL-02, URL-03):

1. Row-click navigation (line ~174):
   `` navigate(`/admin/courses/${course.slug}`) `` → `` navigate(`/quan-tri/khoa-hoc/${course.slug}`) ``

2. Link in header area (line ~110):
   `to="/admin/submissions"` → `to="/quan-tri/bai-nop"`

Do NOT change course data fetching, table structure, form dialogs, or any other logic.
  </action>
  <verify>
    <automated>grep -n "'/admin/courses\|'/admin/submissions\|/admin/courses\|/admin/submissions" src/pages/admin/CoursesPage.tsx</automated>
  </verify>
  <done>Zero lines returned — no English admin URL strings remain in CoursesPage.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Update ChaptersPage.tsx and LessonsPage.tsx URLs</name>
  <files>src/pages/admin/ChaptersPage.tsx, src/pages/admin/LessonsPage.tsx</files>
  <action>
**ChaptersPage.tsx** — 2 changes (per URL-02, URL-03):

1. Chapter row-click navigate (line ~207):
   `` navigate(`/admin/courses/${courseSlug}/chapters/${chapter.slug}`) ``
   → `` navigate(`/quan-tri/khoa-hoc/${courseSlug}/chuong/${chapter.slug}`) ``
   
   NOTE: The static segment `chapters` is renamed to `chuong`. The dynamic param `${chapter.slug}` stays unchanged.

2. Breadcrumb back to course list (line ~132):
   `<Link to="/admin/courses">` → `<Link to="/quan-tri/khoa-hoc">`

**LessonsPage.tsx** — 2 changes (per URL-03):

1. Breadcrumb root link (line ~149):
   `<Link to="/admin/courses">` → `<Link to="/quan-tri/khoa-hoc">`

2. Breadcrumb parent link (line ~155):
   `` <Link to={`/admin/courses/${courseSlug}`}> `` → `` <Link to={`/quan-tri/khoa-hoc/${courseSlug}`}> ``

Do NOT change any data fetching, drag-to-reorder logic, chapter/lesson form dialogs, or other code.
  </action>
  <verify>
    <automated>grep -n "'/admin/courses\|/admin/courses\|/chapters/" src/pages/admin/ChaptersPage.tsx src/pages/admin/LessonsPage.tsx</automated>
  </verify>
  <done>Zero lines returned — no English admin URL strings remain in either file. The `chapters` path segment is replaced with `chuong`.</done>
</task>

</tasks>

<verification>
```bash
# Confirm no English admin URLs remain
grep -n "'/admin/courses\|/admin/courses\|/admin/submissions\|/chapters/" \
  src/pages/admin/CoursesPage.tsx \
  src/pages/admin/ChaptersPage.tsx \
  src/pages/admin/LessonsPage.tsx
# Must return 0 lines
```
</verification>

<success_criteria>
- CoursesPage.tsx: Row-click navigates to `/quan-tri/khoa-hoc/:slug`; header link goes to `/quan-tri/bai-nop`
- ChaptersPage.tsx: Chapter drill-in uses `chuong` segment; breadcrumb links to `/quan-tri/khoa-hoc`
- LessonsPage.tsx: Both breadcrumb levels use Vietnamese paths
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-url-standardization/09-04-SUMMARY.md`
</output>
