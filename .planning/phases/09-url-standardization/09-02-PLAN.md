---
phase: 09-url-standardization
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/pages/Login.tsx
  - src/pages/Register.tsx
  - src/pages/NotFound.tsx
autonomous: true
requirements: [URL-02, URL-03]

must_haves:
  truths:
    - "After login, admins navigate to /quan-tri/nguoi-dung, teachers to /quan-tri/bai-nop, students to /khoa-hoc"
    - "After registration, admins navigate to /quan-tri/nguoi-dung, students to /khoa-hoc"
    - "Login page links to /dang-ky; Register page links back to /dang-nhap"
    - "NotFound page links students to /khoa-hoc, others to /"
  artifacts:
    - path: "src/pages/Login.tsx"
      provides: "Login page with role-based post-login navigation"
      contains: "/quan-tri/nguoi-dung, /quan-tri/bai-nop, /khoa-hoc, /dang-ky"
    - path: "src/pages/Register.tsx"
      provides: "Registration page with post-registration navigation"
      contains: "/quan-tri/nguoi-dung, /khoa-hoc, /dang-nhap"
    - path: "src/pages/NotFound.tsx"
      provides: "404 page with role-aware home link"
      contains: "/khoa-hoc"
  key_links:
    - from: "src/pages/Login.tsx"
      to: "role-based routes"
      via: "navigate() in useEffect"
      pattern: "navigate\\('/quan-tri|navigate\\('/khoa-hoc"
---

<objective>
Update all `navigate()` calls and `<Link to>` values in the three auth/shared pages — Login, Register, NotFound — to use Vietnamese URLs.

Purpose: After Plan 01 defines the new route paths, these pages must navigate to those paths. Without this, logging in redirects to dead English URLs.

Output: Login.tsx (4 changes), Register.tsx (3 changes), NotFound.tsx (1 change) — all pointing to Vietnamese URLs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-url-standardization/09-CONTEXT.md
@.planning/phases/09-url-standardization/09-RESEARCH.md
</context>

<interfaces>
<!-- Route paths now defined in App.tsx (after Plan 01) -->
/dang-nhap     → was /login
/dang-ky       → was /register
/khoa-hoc      → was /courses
/quan-tri/nguoi-dung    → was /admin/users
/quan-tri/bai-nop       → was /admin/submissions
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Login.tsx and Register.tsx navigate/link strings</name>
  <files>src/pages/Login.tsx, src/pages/Register.tsx</files>
  <action>
**Login.tsx** — 4 changes (per URL-02):

In the `useEffect` role-based redirect block:
- `navigate('/admin/users')` → `navigate('/quan-tri/nguoi-dung')` (admin branch)
- `navigate('/admin/submissions')` → `navigate('/quan-tri/bai-nop')` (teacher branch)
- `navigate('/courses')` → `navigate('/khoa-hoc')` (student branch)

In the JSX register link:
- `<Link to="/register">` → `<Link to="/dang-ky">`

**Register.tsx** — 3 changes (per URL-02):

In the `useEffect` role-based redirect block:
- `navigate('/admin/users')` → `navigate('/quan-tri/nguoi-dung')` (admin branch)
- `navigate('/courses')` → `navigate('/khoa-hoc')` (student branch)

In the JSX login link:
- `<Link to="/login">` → `<Link to="/dang-nhap">`

Do NOT change form logic, validation, Supabase auth calls, role checks, or anything else — only the URL strings.
  </action>
  <verify>
    <automated>grep -n "'/login\|'/register\|'/courses\|'/admin/users\|'/admin/submissions" src/pages/Login.tsx src/pages/Register.tsx</automated>
  </verify>
  <done>Zero lines returned — no English URL strings remain in either file. All navigate() and Link to= values use Vietnamese paths.</done>
</task>

<task type="auto">
  <name>Task 2: Update NotFound.tsx homeLink</name>
  <files>src/pages/NotFound.tsx</files>
  <action>
**NotFound.tsx** — 1 change (per URL-03):

Find the role-aware `homeLink` constant:
```tsx
// Before
const homeLink = profile?.role === 'student' ? '/courses' : '/';

// After
const homeLink = profile?.role === 'student' ? '/khoa-hoc' : '/';
```

Do NOT change any other logic, styling, or text content.
  </action>
  <verify>
    <automated>grep -n "'/courses'" src/pages/NotFound.tsx</automated>
  </verify>
  <done>Zero lines returned — `/courses` string is gone from NotFound.tsx. The homeLink now uses `/khoa-hoc` for students.</done>
</task>

</tasks>

<verification>
```bash
# Confirm no English URLs remain across all three files
grep -n "'/login\|'/register\|'/courses\|'/admin/users\|'/admin/submissions" \
  src/pages/Login.tsx src/pages/Register.tsx src/pages/NotFound.tsx
# Must return 0 lines
```
</verification>

<success_criteria>
- Login.tsx: 4 URL strings updated; post-login navigation goes to Vietnamese routes
- Register.tsx: 3 URL strings updated; post-registration navigation goes to Vietnamese routes
- NotFound.tsx: 1 URL string updated; student homeLink is `/khoa-hoc`
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-url-standardization/09-02-SUMMARY.md`
</output>
