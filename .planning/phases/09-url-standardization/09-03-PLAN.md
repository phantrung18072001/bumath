---
phase: 09-url-standardization
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/components/admin/AdminLayout.tsx
  - src/pages/admin/GradingPage.tsx
  - src/pages/admin/SubmissionsPage.tsx
autonomous: true
requirements: [URL-01, URL-02, URL-03]

must_haves:
  truths:
    - "Admin sidebar nav links use Vietnamese URLs for all 3 items"
    - "GradingPage back/breadcrumb links use /quan-tri/bai-nop"
    - "SubmissionsPage row click navigates to /quan-tri/bai-nop/:id"
  artifacts:
    - path: "src/components/admin/AdminLayout.tsx"
      provides: "Admin sidebar navigation"
      contains: "/quan-tri/nguoi-dung, /quan-tri/khoa-hoc, /quan-tri/bai-nop"
    - path: "src/pages/admin/GradingPage.tsx"
      provides: "Individual submission grading page"
      contains: "/quan-tri/bai-nop"
    - path: "src/pages/admin/SubmissionsPage.tsx"
      provides: "Submissions list page with row-click navigation"
      contains: "/quan-tri/bai-nop/"
  key_links:
    - from: "src/components/admin/AdminLayout.tsx"
      to: "/quan-tri/nguoi-dung, /quan-tri/khoa-hoc, /quan-tri/bai-nop"
      via: "navItems array `to:` values + location.pathname.startsWith()"
      pattern: "to: '/quan-tri"
---

<objective>
Update all URL strings in AdminLayout.tsx (nav items), GradingPage.tsx (breadcrumbs + back button), and SubmissionsPage.tsx (row-click navigate).

Purpose: Admin sidebar navigation is the primary way admin/teacher users move between sections. Broken nav links here make the admin interface unusable.

Output: AdminLayout.tsx (3 nav `to:` values), GradingPage.tsx (3 link/navigate occurrences), SubmissionsPage.tsx (1 navigate occurrence).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-url-standardization/09-CONTEXT.md
@.planning/phases/09-url-standardization/09-RESEARCH.md
</context>

<interfaces>
<!-- Route paths now defined in App.tsx (after Plan 01) -->
/quan-tri/nguoi-dung      → was /admin/users
/quan-tri/khoa-hoc        → was /admin/courses
/quan-tri/bai-nop         → was /admin/submissions
/quan-tri/bai-nop/:id     → was /admin/submissions/:id

<!-- AdminLayout active-link detection note -->
location.pathname.startsWith(to) is used for active highlighting.
New paths (/quan-tri/nguoi-dung, /quan-tri/khoa-hoc, /quan-tri/bai-nop) are unique
prefixes — the startsWith() active detection continues to work correctly after rename.
No additional changes needed in the active-detection logic.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update AdminLayout.tsx nav item URLs</name>
  <files>src/components/admin/AdminLayout.tsx</files>
  <action>
**AdminLayout.tsx** — 3 changes (per URL-01, URL-03):

In the `navItems` array, update the 3 `to:` values:
- `to: '/admin/users'` → `to: '/quan-tri/nguoi-dung'`
- `to: '/admin/courses'` → `to: '/quan-tri/khoa-hoc'`
- `to: '/admin/submissions'` → `to: '/quan-tri/bai-nop'`

Do NOT change label text, icon references, the `location.pathname.startsWith(to)` active detection logic, or any other code. Only the 3 string values in `to:` change.

The `startsWith()` active detection continues to work correctly — the new Vietnamese paths are still unique prefixes that don't shadow each other.
  </action>
  <verify>
    <automated>grep -n "'/admin/users\|'/admin/courses\|'/admin/submissions" src/components/admin/AdminLayout.tsx</automated>
  </verify>
  <done>Zero lines returned — no English admin URL strings remain in AdminLayout.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Update GradingPage.tsx and SubmissionsPage.tsx URLs</name>
  <files>src/pages/admin/GradingPage.tsx, src/pages/admin/SubmissionsPage.tsx</files>
  <action>
**GradingPage.tsx** — 3 changes (per URL-02, URL-03):
- Line ~107: `navigate('/admin/submissions')` → `navigate('/quan-tri/bai-nop')`
- Line ~131: `to="/admin/submissions"` (breadcrumb Link) → `to="/quan-tri/bai-nop"`
- Line ~146: `to="/admin/submissions"` (back Link) → `to="/quan-tri/bai-nop"`

**SubmissionsPage.tsx** — 1 change (per URL-02):
- Line ~217: `` navigate(`/admin/submissions/${row.id}`) `` → `` navigate(`/quan-tri/bai-nop/${row.id}`) ``

Do NOT change any data fetching, grading logic, table structure, or component wiring. Only URL strings change.
  </action>
  <verify>
    <automated>grep -n "'/admin/submissions\|/admin/submissions" src/pages/admin/GradingPage.tsx src/pages/admin/SubmissionsPage.tsx</automated>
  </verify>
  <done>Zero lines returned — no English `/admin/submissions` strings remain in either file.</done>
</task>

</tasks>

<verification>
```bash
# Confirm no English admin URLs remain across all three files
grep -n "'/admin/users\|'/admin/courses\|'/admin/submissions\|/admin/submissions" \
  src/components/admin/AdminLayout.tsx \
  src/pages/admin/GradingPage.tsx \
  src/pages/admin/SubmissionsPage.tsx
# Must return 0 lines
```
</verification>

<success_criteria>
- AdminLayout.tsx: 3 nav `to:` values use Vietnamese paths; sidebar active-highlighting still works
- GradingPage.tsx: 3 URL strings updated; breadcrumbs and back button navigate correctly
- SubmissionsPage.tsx: 1 navigate call updated; row-click opens correct grading detail URL
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-url-standardization/09-03-SUMMARY.md`
</output>
