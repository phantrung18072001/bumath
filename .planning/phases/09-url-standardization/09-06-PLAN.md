---
phase: 09-url-standardization
plan: 06
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/pages/student/CourseDetailPage.tsx
  - src/components/landing/Header.tsx
  - src/components/landing/HeroSection.tsx
  - src/components/landing/ClassGrid.tsx
  - src/components/landing/IntensiveSection.tsx
autonomous: true
requirements: [URL-01, URL-02, URL-03]

must_haves:
  truths:
    - "CourseDetailPage login CTA links go to /dang-nhap"
    - "Landing Header nav links use Vietnamese URLs"
    - "HeroSection CTA links to /khoa-hoc or /danh-muc depending on auth state"
    - "ClassGrid grade cards link to /danh-muc?lop=7/8/9"
    - "IntensiveSection CTA links to /danh-muc?lop=nang-cao"
  artifacts:
    - path: "src/pages/student/CourseDetailPage.tsx"
      provides: "Course detail page with lesson viewer"
      contains: "/dang-nhap, /khoa-hoc"
    - path: "src/components/landing/Header.tsx"
      provides: "Landing page navigation"
      contains: "/danh-muc, /khoa-hoc, /quan-tri/nguoi-dung, /dang-nhap, /dang-ky"
    - path: "src/components/landing/HeroSection.tsx"
      provides: "Landing hero with CTA button"
      contains: "/khoa-hoc, /danh-muc"
    - path: "src/components/landing/ClassGrid.tsx"
      provides: "Grade-level course cards on landing page"
      contains: "/danh-muc?lop="
    - path: "src/components/landing/IntensiveSection.tsx"
      provides: "Advanced/intensive course section on landing"
      contains: "/danh-muc?lop=nang-cao"
  key_links:
    - from: "src/components/landing/ClassGrid.tsx"
      to: "/danh-muc?lop={level}"
      via: "c.level property (values: 7, 8, 9)"
      pattern: "danh-muc\\?lop="
---

<objective>
Update all URL strings in CourseDetailPage.tsx (2-3 login/back links) and the four landing page components (Header, HeroSection, ClassGrid, IntensiveSection — 7+ links including the ClassGrid ?lop= query param).

Purpose: The landing page is the first thing visitors see. Broken links here would block unauthenticated visitors from reaching login/register, and authenticated users from reaching their courses.

Output: CourseDetailPage.tsx (2-3 changes), Header.tsx (7 changes), HeroSection.tsx (1 change), ClassGrid.tsx (1 link change using c.level), IntensiveSection.tsx (1 change).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-url-standardization/09-CONTEXT.md
@.planning/phases/09-url-standardization/09-RESEARCH.md
</context>

<interfaces>
<!-- Route paths now defined in App.tsx (after Plan 01) -->
/dang-nhap              → was /login
/dang-ky                → was /register
/khoa-hoc               → was /courses
/danh-muc               → was /catalogue
/quan-tri/nguoi-dung    → was /admin/users

<!-- ClassGrid data shape (existing, do NOT change) -->
classes array items have: { level: 7 | 8 | 9, grade: "grade_7" | "grade_8" | "grade_9", ... }
- Use c.level directly for ?lop= values: level 7 → ?lop=7, level 8 → ?lop=8, level 9 → ?lop=9
- Do NOT use c.grade (it has DB enum values like "grade_7" which don't match new ?lop= values)
- Change: to={`/catalogue?grade=${c.grade}`} → to={`/danh-muc?lop=${c.level}`}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update CourseDetailPage.tsx login and back links</name>
  <files>src/pages/student/CourseDetailPage.tsx</files>
  <action>
**CourseDetailPage.tsx** — 2-3 changes (per URL-02, URL-03):

1. Unauthenticated CTA login link (line ~294):
   `<Link to="/login">` → `<Link to="/dang-nhap">`

2. Second login link (line ~338):
   `<Link to="/login">` → `<Link to="/dang-nhap">`

3. Back link to courses list (line ~138, if present):
   `to="/courses"` → `to="/khoa-hoc"`
   (Check this line — it may link back to the courses list)

Do NOT change lesson viewer logic, progress tracking, submission area, enrollment checks, or any other functionality.
  </action>
  <verify>
    <automated>grep -n "'/login\|'/courses\|to=\"/login\|to=\"/courses" src/pages/student/CourseDetailPage.tsx</automated>
  </verify>
  <done>Zero lines returned — no English URL strings remain in CourseDetailPage.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Update all four landing page components</name>
  <files>
    src/components/landing/Header.tsx,
    src/components/landing/HeroSection.tsx,
    src/components/landing/ClassGrid.tsx,
    src/components/landing/IntensiveSection.tsx
  </files>
  <action>
**Header.tsx** — 7 changes (per URL-01, URL-03):

staticNavItems array:
- `to: "/catalogue"` → `to: "/danh-muc"`

Authenticated user links (desktop + mobile — 2 instances each):
- `<Link to="/courses">` → `<Link to="/khoa-hoc">` (lines ~63 desktop, ~166 mobile)
- `<Link to="/admin/users">` → `<Link to="/quan-tri/nguoi-dung">` (lines ~71 desktop, ~174 mobile)

Unauthenticated links (desktop + mobile — 2 instances each):
- `<Link to="/login">` → `<Link to="/dang-nhap">` (lines ~90 desktop, ~193 mobile)
- `<Link to="/register">` → `<Link to="/dang-ky">` (lines ~95 desktop, ~196 mobile)

Do NOT change layout, menu toggle logic, scroll behavior, or visual design.

**HeroSection.tsx** — 1 change (per URL-03):

The CTA button href:
- `<Link to={isAuthenticated ? "/courses" : "/catalogue"}>` → `<Link to={isAuthenticated ? "/khoa-hoc" : "/danh-muc"}>`

Both ternary branches must be updated.

**ClassGrid.tsx** — 1 change (per URL-03):

The grade card Link:
- `to={`/catalogue?grade=${c.grade}`}` → `to={`/danh-muc?lop=${c.level}`}`

CRITICAL: Use `c.level` (values: 7, 8, 9), NOT `c.grade` (values: "grade_7", "grade_8", "grade_9").
Using `c.grade` would produce `/danh-muc?lop=grade_7` which does not match LOI_MAP keys in CataloguePage.

**IntensiveSection.tsx** — 1 change (per URL-03):

The CTA button:
- `<Link to="/catalogue?grade=advanced">` → `<Link to="/danh-muc?lop=nang-cao">`
  </action>
  <verify>
    <automated>grep -n "'/login\|'/register\|'/courses\|'/catalogue\|'/admin/users\|/catalogue\|/login\|/register\|/courses" \
  src/components/landing/Header.tsx \
  src/components/landing/HeroSection.tsx \
  src/components/landing/ClassGrid.tsx \
  src/components/landing/IntensiveSection.tsx</automated>
  </verify>
  <done>Zero lines returned — no English URL strings remain in any of the four landing components. ClassGrid uses c.level for ?lop= values.</done>
</task>

</tasks>

<verification>
```bash
# Confirm no English URLs remain across all five files
grep -n "'/login\|'/register\|'/courses\|'/catalogue\|'/admin/users\|/catalogue\?grade\|/courses\|to=\"/login" \
  src/pages/student/CourseDetailPage.tsx \
  src/components/landing/Header.tsx \
  src/components/landing/HeroSection.tsx \
  src/components/landing/ClassGrid.tsx \
  src/components/landing/IntensiveSection.tsx
# Must return 0 lines

# Confirm ClassGrid uses c.level (not c.grade) for ?lop=
grep -n "lop=\${c\." src/components/landing/ClassGrid.tsx
# Must show: lop=${c.level}
```
</verification>

<success_criteria>
- CourseDetailPage.tsx: 2-3 login/back links use Vietnamese paths
- Header.tsx: All 7 nav links use Vietnamese paths (desktop AND mobile variants)
- HeroSection.tsx: CTA ternary uses `/khoa-hoc` and `/danh-muc`
- ClassGrid.tsx: Grade cards link to `/danh-muc?lop=${c.level}` (not c.grade)
- IntensiveSection.tsx: Advanced CTA links to `/danh-muc?lop=nang-cao`
- No visual/layout changes; no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-url-standardization/09-06-SUMMARY.md`
</output>
