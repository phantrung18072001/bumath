---
phase: 09-url-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/auth/ProtectedRoute.tsx
autonomous: true
requirements: [URL-01, URL-02, URL-03]

must_haves:
  truths:
    - "All 11 route path definitions in App.tsx use Vietnamese slugs"
    - "Unauthenticated users redirected to /dang-nhap (not 404)"
    - "Post-login role redirects use Vietnamese URLs"
  artifacts:
    - path: "src/App.tsx"
      provides: "Authoritative route definitions for the entire SPA"
      contains: "/dang-nhap, /dang-ky, /quan-tri/nguoi-dung, /quan-tri/khoa-hoc, /quan-tri/bai-nop, /khoa-hoc, /danh-muc"
    - path: "src/components/auth/ProtectedRoute.tsx"
      provides: "Role-based redirect guard for protected routes"
      contains: "/dang-nhap, /khoa-hoc, /quan-tri/bai-nop"
  key_links:
    - from: "src/components/auth/ProtectedRoute.tsx"
      to: "/dang-nhap"
      via: "<Navigate to> prop"
      pattern: 'Navigate to="/dang-nhap"'
    - from: "src/components/auth/ProtectedRoute.tsx"
      to: "/khoa-hoc, /quan-tri/bai-nop"
      via: "redirectFor() return values"
      pattern: "return '/khoa-hoc'"
---

<objective>
Rename all 11 English route path definitions in App.tsx to Vietnamese, and update ProtectedRoute.tsx's 3 hardcoded redirect strings.

Purpose: App.tsx is the single source of truth for all route paths. If these are wrong, every other URL reference is broken regardless of what the leaf files say. ProtectedRoute.tsx must be done alongside it — an unauthenticated user hitting any protected route would redirect to the dead `/login` path, hitting NotFound.

Output: App.tsx with 11 renamed paths; ProtectedRoute.tsx with 3 renamed redirect strings.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-url-standardization/09-CONTEXT.md
@.planning/phases/09-url-standardization/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all route path definitions in App.tsx</name>
  <files>src/App.tsx</files>
  <action>
Replace the 11 English path strings (per URL-01, URL-02, URL-03 — decisions locked in CONTEXT.md).
Do NOT change component imports, element props, layout wrappers, or anything else — only the `path=` strings.

Exact replacements (line numbers from RESEARCH.md):
- `path="/login"` → `path="/dang-nhap"`
- `path="/register"` → `path="/dang-ky"`
- `path="/admin/users"` → `path="/quan-tri/nguoi-dung"`
- `path="/admin/courses"` → `path="/quan-tri/khoa-hoc"`
- `path="/admin/courses/:courseSlug"` → `path="/quan-tri/khoa-hoc/:courseSlug"`
- `path="/admin/courses/:courseSlug/chapters/:chapterSlug"` → `path="/quan-tri/khoa-hoc/:courseSlug/chuong/:chapterSlug"`
- `path="/admin/submissions"` → `path="/quan-tri/bai-nop"`
- `path="/admin/submissions/:submissionId"` → `path="/quan-tri/bai-nop/:submissionId"`
- `path="/courses"` → `path="/khoa-hoc"`
- `path="/courses/:courseSlug"` → `path="/khoa-hoc/:courseSlug"`
- `path="/catalogue"` → `path="/danh-muc"`

NOTE: `/pending` route does NOT exist in App.tsx — do NOT add it. Skip entirely.

Dynamic param names (`:courseSlug`, `:chapterSlug`, `:submissionId`) stay unchanged — they are internal identifiers, not user-visible (per CONTEXT.md locked decision).
  </action>
  <verify>
    <automated>grep -n 'path="/' src/App.tsx | grep -v 'dang-nhap\|dang-ky\|quan-tri\|khoa-hoc\|danh-muc\|^\*\|index\|/\*' | grep -E '(login|register|admin|courses|catalogue)'</automated>
  </verify>
  <done>The grep above returns zero lines — no English route paths remain. All 11 paths use Vietnamese slugs.</done>
</task>

<task type="auto">
  <name>Task 2: Update ProtectedRoute.tsx redirect strings</name>
  <files>src/components/auth/ProtectedRoute.tsx</files>
  <action>
Update the 3 hardcoded URL strings in ProtectedRoute.tsx (per URL-02):

1. `redirectFor()` teacher branch: `'/admin/submissions'` → `'/quan-tri/bai-nop'`
2. `redirectFor()` student branch: `'/courses'` → `'/khoa-hoc'`
3. `<Navigate to="/login" replace />` → `<Navigate to="/dang-nhap" replace />`

Do NOT change any logic, conditions, role checks, or the function signature. Only the 3 string values change.

Critical: If the Navigate still points to `/login`, unauthenticated users hitting any protected route will land on NotFound instead of the login page.
  </action>
  <verify>
    <automated>grep -n "'/login\|'/courses\|'/admin/submissions\|/login\|/courses\|/admin/submissions" src/components/auth/ProtectedRoute.tsx | grep -v 'dang-nhap\|khoa-hoc\|quan-tri'</automated>
  </verify>
  <done>The grep returns zero lines — no old English redirect strings remain in ProtectedRoute.tsx. All 3 redirects use Vietnamese paths.</done>
</task>

</tasks>

<verification>
```bash
# Confirm App.tsx has no English route paths left
grep -n 'path="/' src/App.tsx | grep -E '"/(login|register|admin/|courses|catalogue)'

# Confirm ProtectedRoute has no English redirects
grep -n '/login\|/courses\|/admin/submissions' src/components/auth/ProtectedRoute.tsx

# Both commands should return 0 lines
```
</verification>

<success_criteria>
- `src/App.tsx`: All 11 routes renamed to Vietnamese; no English path strings remain; `/pending` was not present and not added
- `src/components/auth/ProtectedRoute.tsx`: All 3 redirect strings renamed to Vietnamese
- No TypeScript errors introduced (`yarn tsc --noEmit` passes)
- Wave 2 plans (02–06) can now run in parallel — they depend on these route definitions being correct
</success_criteria>

<output>
After completion, create `.planning/phases/09-url-standardization/09-01-SUMMARY.md`
</output>
