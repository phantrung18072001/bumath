---
phase: 18-mock-exam-system
plan: 04
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - .planning/phases/18-mock-exam-system/18-04-UI-DESIGN.md
  - src/pages/student/MockExamsPage.tsx
  - src/pages/student/MockExamAttemptPage.tsx
  - src/components/student/ExamCountdown.tsx
  - src/components/student/ExamQuestionCard.tsx
  - src/components/student/ExamSubmitPanel.tsx
  - src/lib/api/exams.ts
  - src/App.tsx
  - src/pages/student/MockExamsPage.test.tsx
  - src/pages/student/MockExamAttemptPage.test.tsx
autonomous: true
requirements: [EXAM-03, EXAM-04, EXAM-05, EXAM-06]
must_haves:
  truths:
    - "Student sees currently open exam sessions and can enter one."
    - "Countdown is based on server timestamps from attempt/session data (not local duration-only timer)."
    - "Second attempt is blocked and shown with explicit one-attempt message."
    - "Submit returns immediate score and per-question correctness markers without revealing canonical answers."
    - "Late submit is rejected with clear feedback from server-side enforcement."
  artifacts:
    - path: "src/pages/student/MockExamsPage.tsx"
      provides: "Open exam list and start CTA"
    - path: "src/pages/student/MockExamAttemptPage.tsx"
      provides: "Attempt runtime, autosave, submit, result view"
  key_links:
    - from: "MockExamAttemptPage"
      to: "submitExamAttempt API"
      via: "mutation"
      pattern: "submitExamAttempt"
---

<objective>
Ship student mock exam experience end-to-end: discover open sessions, take one timed attempt with reconnect recovery, and receive immediate score.

Purpose: satisfy EXAM-03/04/05/06 with server-authoritative behavior.
Output: student pages/components, route wiring, dependency install, passing tests.
</objective>

<execution_context>
@/home/trungpd-teko-pro/.codex/get-shit-done/workflows/execute-plan.md
@/home/trungpd-teko-pro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-mock-exam-system/18-02-SUMMARY.md
@src/pages/student/CourseDetailPage.tsx
@src/components/student/SubmissionArea.tsx
@src/lib/api/exams.ts
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 0: Run mandatory UI design-system step before student exam UI work</name>
  <read_first>
    - CLAUDE.md
    - .planning/phases/18-mock-exam-system/18-CONTEXT.md
  </read_first>
  <files>.planning/phases/18-mock-exam-system/18-04-UI-DESIGN.md</files>
  <action>
    Execute the required design-system query from CLAUDE.md before building student exam pages:
    - `python3 .claude/skills/ui-ux-pro-max/scripts/search.py "student timed mock exam countdown autosave result" --design-system -p "BuMath"`
    Record selected interaction/visual decisions in `18-04-UI-DESIGN.md` and apply them in subsequent student UI tasks.
  </action>
  <verify>
    <automated>test -s .planning/phases/18-mock-exam-system/18-04-UI-DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - Design-system query is run before UI implementation.
    - Student exam UX checklist decisions are documented for execution consistency.
  </acceptance_criteria>
  <done>Plan complies with mandatory pre-implementation UI design step in CLAUDE.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Build student exam list page and attempt entry route</name>
  <read_first>
    - src/pages/student/CoursesPage.tsx
    - src/App.tsx
    - src/lib/api/exams.ts
  </read_first>
  <files>src/pages/student/MockExamsPage.tsx, src/App.tsx, src/pages/student/MockExamsPage.test.tsx</files>
  <behavior>
    - Test 1: Open sessions render with valid start CTA.
    - Test 2: Empty state renders clear message when no session is currently open.
  </behavior>
  <action>
    Implement student exam listing page:
    - fetch open/published sessions via exam API,
    - show status/time window/type,
    - route to attempt page on start.
    Add route under student-protected area and activate existing test stubs.
  </action>
  <verify>
    <automated>yarn test src/pages/student/MockExamsPage.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - Student route exists for exam list and is accessible to approved users.
    - Open sessions appear with start CTA.
    - Empty and error states are explicit and non-crashing.
  </acceptance_criteria>
  <done>Students can discover and enter available mock exams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build timed attempt runtime with autosave, one-attempt gate, and submit result</name>
  <read_first>
    - src/pages/student/MockExamAttemptPage.test.tsx
    - src/lib/api/exams.ts
    - .planning/phases/18-mock-exam-system/18-CONTEXT.md
  </read_first>
  <files>src/pages/student/MockExamAttemptPage.tsx, src/components/student/ExamCountdown.tsx, src/components/student/ExamQuestionCard.tsx, src/components/student/ExamSubmitPanel.tsx, src/lib/api/exams.ts, src/pages/student/MockExamAttemptPage.test.tsx</files>
  <behavior>
    - Test 1: When submit succeeds, page shows raw score and score_10 immediately (EXAM-05).
    - Test 2: Late submit error shows deterministic rejection message from server check (EXAM-06).
  </behavior>
  <action>
    Implement attempt flow per D-05..D-12:
    - call `startExamAttempt` on first entry; if existing attempt exists, restore state and block second-start path (D-07, D-08),
    - render questions in fixed order (D-03),
    - autosave answers debounced and on interval/focus-loss; disallow save after submitted_at,
    - countdown derived from `started_at` + session `ends_at`; auto-submit when timer reaches zero (D-06),
    - submit via API and display immediate result: raw score + score/10 + per-question correct/incorrect (no answer key reveal).
    - import KaTeX stylesheet once in `src/index.css` so exam question rendering is styled consistently.
    Activate tests and add edge-case checks for reconnect and duplicate-submit guard.
  </action>
  <verify>
    <automated>yarn test src/pages/student/MockExamAttemptPage.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - Attempt page restores existing in-progress attempt after refresh.
    - Second attempt start is blocked with explicit one-attempt message.
    - Timer auto-submits at expiration.
    - Immediate result view includes raw score and score_10.
    - Late submit path shows server-rejected error message.
  </acceptance_criteria>
  <done>Student attempt flow fulfills EXAM-04/05/06 with server-authoritative outcomes.</done>
</task>

</tasks>

<verification>
Run student exam tests and targeted admin exam detail test after KaTeX install; run `yarn lint` on modified TSX files.
</verification>

<success_criteria>
Students can reliably take one timed exam attempt in-window, recover after refresh, and see immediate scoring; late submissions are rejected server-side.
</success_criteria>

<output>
After completion, create `.planning/phases/18-mock-exam-system/18-04-SUMMARY.md`.
</output>
