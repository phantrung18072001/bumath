---
phase: 18-mock-exam-system
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/lib/api/exams.ts
  - src/lib/api/exams.test.ts
  - src/pages/admin/ExamSessionsPage.test.tsx
  - src/pages/admin/ExamSessionDetailPage.test.tsx
  - src/pages/student/MockExamsPage.test.tsx
  - src/pages/student/MockExamAttemptPage.test.tsx
  - package.json
  - yarn.lock
autonomous: true
requirements: [EXAM-01, EXAM-02, EXAM-03, EXAM-04, EXAM-05, EXAM-06]
must_haves:
  truths:
    - "Client API is a thin wrapper over DB-enforced RPCs, not business logic duplication."
    - "Wave 0 tests define expected behavior for admin creation, student attempt lifecycle, one-attempt and deadline rejection."
    - "All downstream UI plans import one stable exam API contract."
  artifacts:
    - path: "src/lib/api/exams.ts"
      provides: "Typed exam API functions and DTOs"
    - path: "src/lib/api/exams.test.ts"
      provides: "Automated API contract/error-mapping tests"
  key_links:
    - from: "src/lib/api/exams.ts"
      to: "submit_exam_attempt RPC"
      via: "supabase.rpc"
      pattern: "submit_exam_attempt"
---

<objective>
Define interface-first contracts for all exam operations and create Wave 0 test scaffolds so implementation plans can execute with zero ambiguity.

Purpose: lock public API and expected behavior before UI wiring.
Output: `exams.ts` + focused test scaffolds.
</objective>

<execution_context>
@/home/trungpd-teko-pro/.codex/get-shit-done/workflows/execute-plan.md
@/home/trungpd-teko-pro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-mock-exam-system/18-01-SUMMARY.md
@.planning/phases/18-mock-exam-system/18-RESEARCH.md
@src/lib/api/submissions.ts
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create exam API contract module with typed exports</name>
  <read_first>
    - .planning/phases/18-mock-exam-system/18-01-SUMMARY.md
    - src/lib/api/submissions.ts
    - src/lib/supabase.ts
  </read_first>
  <files>src/lib/api/exams.ts</files>
  <behavior>
    - Test 1: start attempt returns existing/new attempt and maps duplicate attempt conflict to user-facing one-attempt error.
    - Test 2: submit maps DB late-submission error to deterministic application error code.
  </behavior>
  <action>
    Create `src/lib/api/exams.ts` with:
    - Types: `ExamSession`, `ExamQuestion`, `ExamAttempt`, `ExamSubmitResult`, `ExamSessionStatus`.
    - Admin functions: `fetchExamSessionsForAdmin`, `createExamSession`, `updateExamSession`, `publishExamSession`, `deleteExamSession`, `fetchExamQuestions`, `upsertExamQuestion`.
    - Student functions: `fetchOpenExamSessionsForStudent`, `startExamAttempt`, `saveExamAttemptAnswers`, `submitExamAttempt`, `fetchMyExamAttempt`.
    - Ensure all writes go through RPCs/functions from plan 01 for critical gates (D-08, D-12).
  </action>
  <verify>
    <automated>grep -E "export (async )?function (fetchExamSessionsForAdmin|createExamSession|publishExamSession|fetchOpenExamSessionsForStudent|startExamAttempt|saveExamAttemptAnswers|submitExamAttempt)" src/lib/api/exams.ts | wc -l | grep -qE "^[7-9]|1[0-9]$"</automated>
  </verify>
  <acceptance_criteria>
    - File exports all required admin and student functions.
    - `submitExamAttempt` calls submit RPC, not client-side grading logic.
    - Error mapping for one-attempt and deadline rejection is explicit and stable.
  </acceptance_criteria>
  <done>All exam features have one typed API layer used by both admin and student pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install KaTeX dependencies before downstream UI implementation</name>
  <read_first>
    - package.json
    - .planning/phases/18-mock-exam-system/18-RESEARCH.md
  </read_first>
  <files>package.json, yarn.lock</files>
  <behavior>
    - Test 1: `katex` dependency is present for both admin/student question rendering plans.
    - Test 2: `react-katex` dependency is present before plan 03 and 04 implementation begins.
  </behavior>
  <action>
    Install required packages early in phase execution:
    - run `yarn add katex react-katex`
    This task must complete before UI implementation plans that import KaTeX renderers (explicitly required by 18-03 and 18-04 via `depends_on: ["18-02"]`).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies?.katex||!p.dependencies?.['react-katex']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` includes `katex` and `react-katex` dependencies.
    - `yarn.lock` updated by Yarn 4.
    - Downstream plans can import `react-katex` without ad-hoc install steps.
  </acceptance_criteria>
  <done>Dependency edge for KaTeX is satisfied before any plan needing KaTeX executes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add API contract tests and Wave 0 page test scaffolds</name>
  <read_first>
    - .planning/phases/18-mock-exam-system/18-VALIDATION.md
    - src/test/setup.ts
    - src/lib/api/exams.ts
  </read_first>
  <files>src/lib/api/exams.test.ts, src/pages/admin/ExamSessionsPage.test.tsx, src/pages/admin/ExamSessionDetailPage.test.tsx, src/pages/student/MockExamsPage.test.tsx, src/pages/student/MockExamAttemptPage.test.tsx</files>
  <behavior>
    - Test 1: submit error after ends_at maps to `EXAM_DEADLINE_PASSED`.
    - Test 2: duplicate start maps to `EXAM_ATTEMPT_ALREADY_EXISTS`.
  </behavior>
  <action>
    Implement `src/lib/api/exams.test.ts` with active tests for API/error-mapping.
    Create/extend page-level test files as Wave 0 stubs (`it.skip`) referencing:
    - admin lifecycle (draft/publish restrictions),
    - question authoring (latex/image/answer completeness),
    - student open exam list,
    - attempt countdown/autosave/submit result rendering.
    Keep test titles tagged with requirement IDs for traceability.
  </action>
  <verify>
    <automated>yarn test src/lib/api/exams.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/api/exams.test.ts` passes.
    - All 4 page test files exist and run without failing imports.
    - Wave 0 stubs cover EXAM-01..EXAM-06 explicitly.
  </acceptance_criteria>
  <done>API behavior is test-anchored; UI plans have concrete verification targets.</done>
</task>

</tasks>

<verification>
`yarn test src/lib/api/exams.test.ts --run` and quick run from phase validation document for exam files.
</verification>

<success_criteria>
Interface-first contracts are frozen and validated; downstream implementation can proceed without contract discovery work.
</success_criteria>

<output>
After completion, create `.planning/phases/18-mock-exam-system/18-02-SUMMARY.md`.
</output>
