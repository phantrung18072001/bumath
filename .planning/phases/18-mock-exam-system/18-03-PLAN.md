---
phase: 18-mock-exam-system
plan: 03
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - .planning/phases/18-mock-exam-system/18-03-UI-DESIGN.md
  - src/pages/admin/ExamSessionsPage.tsx
  - src/pages/admin/ExamSessionDetailPage.tsx
  - src/components/admin/ExamSessionFormDialog.tsx
  - src/components/admin/ExamQuestionForm.tsx
  - src/App.tsx
  - src/components/admin/AdminLayout.tsx
  - src/pages/admin/ExamSessionsPage.test.tsx
  - src/pages/admin/ExamSessionDetailPage.test.tsx
autonomous: true
requirements: [EXAM-01, EXAM-02]
must_haves:
  truths:
    - "Admin can create/update exam sessions with type and time window."
    - "Admin can add/edit questions with text/LaTeX/image and exactly one correct choice."
    - "Publish action is blocked in UI when session data is invalid, and server guard remains authoritative."
  artifacts:
    - path: "src/pages/admin/ExamSessionsPage.tsx"
      provides: "Session list, create/edit/publish/delete controls"
    - path: "src/pages/admin/ExamSessionDetailPage.tsx"
      provides: "Question authoring UI"
  key_links:
    - from: "ExamSessionDetailPage"
      to: "upsertExamQuestion API"
      via: "React Query mutation"
      pattern: "upsertExamQuestion"
---

<objective>
Ship admin-facing mock exam management flows: session lifecycle and question authoring.

Purpose: satisfy EXAM-01 and EXAM-02 with production-ready admin UX.
Output: routed admin pages + passing tests.
</objective>

<execution_context>
@/home/trungpd-teko-pro/.codex/get-shit-done/workflows/execute-plan.md
@/home/trungpd-teko-pro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-mock-exam-system/18-02-SUMMARY.md
@src/pages/admin/CoursesPage.tsx
@src/components/admin/CourseFormDialog.tsx
@src/components/admin/AdminLayout.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 0: Run mandatory UI design-system step before implementation</name>
  <read_first>
    - CLAUDE.md
    - .planning/phases/18-mock-exam-system/18-CONTEXT.md
  </read_first>
  <files>.planning/phases/18-mock-exam-system/18-03-UI-DESIGN.md</files>
  <action>
    Execute the required design-system query from CLAUDE.md before building admin exam UI:
    - `python3 .claude/skills/ui-ux-pro-max/scripts/search.py "admin mock exam management session authoring" --design-system -p "BuMath"`
    Capture selected patterns and checklist decisions (contrast/focus/responsive/clickable states/no emoji icons) in `18-03-UI-DESIGN.md`, then implement page UI accordingly.
  </action>
  <verify>
    <automated>test -s .planning/phases/18-mock-exam-system/18-03-UI-DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - Design-system query executed before UI coding.
    - A short design decision note exists and is referenced by implementation tasks.
  </acceptance_criteria>
  <done>Plan complies with mandatory pre-implementation UI design step in CLAUDE.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Build Admin Exam Sessions page and app routing</name>
  <read_first>
    - src/pages/admin/CoursesPage.tsx
    - src/App.tsx
    - src/components/admin/AdminLayout.tsx
    - src/lib/api/exams.ts
  </read_first>
  <files>src/pages/admin/ExamSessionsPage.tsx, src/components/admin/ExamSessionFormDialog.tsx, src/App.tsx, src/components/admin/AdminLayout.tsx, src/pages/admin/ExamSessionsPage.test.tsx</files>
  <behavior>
    - Test 1: Admin creates a draft session with valid start/end and sees it in list.
    - Test 2: Publish action triggers API and refreshes list; invalid session state shows actionable error.
  </behavior>
  <action>
    Implement `ExamSessionsPage` using existing admin-list patterns:
    - Table/cards with status badge (`draft/published/closed`), type (`monthly/quarterly`), starts_at, ends_at.
    - Create/edit dialog with validation for `ends_at > starts_at`.
    - Publish and delete actions honoring D-13..D-16.
    - Add admin nav item and protected route.
    Activate tests previously scaffolded for this page.
  </action>
  <verify>
    <automated>yarn test src/pages/admin/ExamSessionsPage.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `/quan-tri/de-thi` (or project-standard admin exam path) route exists and is protected.
    - Admin can create/edit/publish session from UI.
    - Published sessions show non-draft status immediately after mutation.
  </acceptance_criteria>
  <done>Session lifecycle UI is available and test-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build question authoring page with KaTeX rendering support</name>
  <read_first>
    - src/lib/api/exams.ts
    - src/pages/admin/ExamSessionDetailPage.test.tsx
  </read_first>
  <files>src/pages/admin/ExamSessionDetailPage.tsx, src/components/admin/ExamQuestionForm.tsx, src/pages/admin/ExamSessionDetailPage.test.tsx</files>
  <behavior>
    - Test 1: Question form requires exactly one valid correct choice A/B/C/D.
    - Test 2: Latex content renders preview (or safe fallback) and persists via API.
  </behavior>
  <action>
    Implement session detail page for question management:
    - list ordered questions (fixed order, D-03),
    - create/edit fields: prompt text, optional latex, optional image_url, choices A/B/C/D, correct choice,
    - disable edits when session already started (D-04),
    - keep stable order controls (no randomization).
    Add KaTeX preview wiring via `react-katex`/`katex` and safe fallback for parse errors.
  </action>
  <verify>
    <automated>yarn test src/pages/admin/ExamSessionDetailPage.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - Admin can add/update ordered questions with answer key input.
    - UI enforces one correct answer and clear validation errors.
    - Edits are blocked when session has started.
  </acceptance_criteria>
  <done>EXAM-02 authoring flow is complete and enforced in UI+API contracts.</done>
</task>

</tasks>

<verification>
Run page tests and lint for new admin files.
</verification>

<success_criteria>
Admin can manage exam sessions and author valid MCQ question sets with LaTeX/image support and lifecycle constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/18-mock-exam-system/18-03-SUMMARY.md`.
</output>
