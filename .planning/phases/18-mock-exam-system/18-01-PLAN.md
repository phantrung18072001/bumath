---
phase: 18-mock-exam-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260513_22_mock_exam_system.sql
autonomous: true
requirements: [EXAM-01, EXAM-02, EXAM-04, EXAM-06]
must_haves:
  truths:
    - "Admin can create draft mock exam sessions with start/end windows and publish only when questions+answers are valid (D-13, D-14)."
    - "Students cannot read answer keys directly; answers are separated in exam_question_answers (D-10)."
    - "A student can have only one attempt per session enforced by DB unique constraint (D-08)."
    - "Server rejects late submit by checking DB time against ends_at, not browser clock (D-12)."
  artifacts:
    - path: "supabase/migrations/20260513_22_mock_exam_system.sql"
      provides: "Exam schema, RLS policies, publish/start/save/submit RPCs, constraints"
  key_links:
    - from: "exam_attempts"
      to: "UNIQUE(exam_session_id, user_id)"
      via: "database constraint"
      pattern: "UNIQUE \\(exam_session_id, user_id\\)"
    - from: "submit_exam_attempt RPC"
      to: "exam_sessions.ends_at"
      via: "server-side deadline validation"
      pattern: "now\\(\\) > v_session.ends_at"
---

<objective>
Build the database foundation for mock exams with strict server-side enforcement: session lifecycle, isolated answer keys, one-attempt rule, autosave-safe attempt flow, and grading RPC that returns score payload without leaking canonical answers.

Purpose: downstream API/UI plans depend on these contracts and security guarantees.
Output: one migration file applied to Supabase.
</objective>

<execution_context>
@/home/trungpd-teko-pro/.codex/get-shit-done/workflows/execute-plan.md
@/home/trungpd-teko-pro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-mock-exam-system/18-CONTEXT.md
@.planning/phases/18-mock-exam-system/18-RESEARCH.md
@supabase/migrations/20260504_20_packages_rls_trigger.sql
@supabase/migrations/20260429_16_profiles_rls.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exam schema and constraints per locked decisions</name>
  <read_first>
    - .planning/phases/18-mock-exam-system/18-CONTEXT.md
    - .planning/phases/18-mock-exam-system/18-RESEARCH.md
    - supabase/migrations/20260504_20_packages_rls_trigger.sql
  </read_first>
  <files>supabase/migrations/20260513_22_mock_exam_system.sql</files>
  <action>
    Create migration with:
    - Enums: exam_session_status (`draft`,`published`,`closed`), exam_session_type (`monthly`,`quarterly`), exam_choice (`A`,`B`,`C`,`D`).
    - Tables: `exam_sessions`, `exam_questions`, `exam_question_answers`, `exam_attempts`.
    - `exam_questions` stores content/latex/image_url/order_index only; `exam_question_answers` stores `correct_choice` separately (D-02, D-10).
    - Add `UNIQUE (exam_session_id, user_id)` on `exam_attempts` (D-08).
    - Add checks for `ends_at > starts_at`, non-empty prompt, and `order_index >= 1`.
    - Add index on `(exam_session_id, order_index)` and `(exam_session_id, user_id)`.
  </action>
  <verify>
    <automated>grep -E "CREATE TABLE.*exam_sessions|CREATE TABLE.*exam_questions|CREATE TABLE.*exam_question_answers|CREATE TABLE.*exam_attempts|UNIQUE \\(exam_session_id, user_id\\)" supabase/migrations/20260513_22_mock_exam_system.sql | wc -l | grep -q "^5$"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists with all 4 tables.
    - `exam_question_answers` is a separate table referencing `exam_questions(id)`.
    - `exam_attempts` has unique constraint on `(exam_session_id, user_id)`.
    - Session status and type enums exist.
  </acceptance_criteria>
  <done>Schema supports EXAM-01/02/04 data model and answer separation constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Add RLS and RPCs for publish/start/autosave/submit-and-grade</name>
  <read_first>
    - supabase/migrations/20260513_22_mock_exam_system.sql
    - supabase/migrations/20260429_16_profiles_rls.sql
    - .planning/phases/18-mock-exam-system/18-CONTEXT.md
  </read_first>
  <files>supabase/migrations/20260513_22_mock_exam_system.sql</files>
  <action>
    Extend migration with:
    - RLS: admin/teacher manage sessions+questions+answers; students can read only published/open sessions and their own attempts; students have no SELECT policy on `exam_question_answers`.
    - RPCs (SECURITY DEFINER, search_path public):
      1) `publish_exam_session(p_session_id uuid)` validates at least 1 question and all questions have valid answer (D-14), blocks publish otherwise.
      2) `start_exam_attempt(p_session_id uuid)` inserts attempt with `started_at=now()`, enforces session published/open and one-attempt unique gate (D-05, D-08).
      3) `save_exam_attempt_answers(p_attempt_id uuid, p_answers jsonb)` updates `answers_payload` only when `submitted_at IS NULL`.
      4) `submit_exam_attempt(p_attempt_id uuid)` enforces `now() <= ends_at` (D-12), computes raw score and score/10, writes `submitted_at`, `raw_score`, `score_10`, and returns `{raw_score, score_10, per_question}` where per_question is correct/incorrect only (D-09, D-10, D-11).
    - Lock edit after session started: question/answer updates blocked by trigger or function guard when any attempt exists (D-04, D-16).
  </action>
  <verify>
    <automated>grep -E "CREATE OR REPLACE FUNCTION public.publish_exam_session|start_exam_attempt|save_exam_attempt_answers|submit_exam_attempt|SECURITY DEFINER" supabase/migrations/20260513_22_mock_exam_system.sql | wc -l | grep -qE "^[5-9]|1[0-9]$"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 RPCs exist with `SECURITY DEFINER`.
    - Students cannot select from `exam_question_answers`.
    - `submit_exam_attempt` contains deadline check against `ends_at`.
    - Publish RPC validates minimum question+answer completeness.
    - Mutation path prevents editing after attempts have started.
  </acceptance_criteria>
  <done>DB layer fully enforces EXAM-06 and one-attempt fairness rules regardless of client behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration and capture DB verification evidence</name>
  <read_first>
    - supabase/migrations/20260513_22_mock_exam_system.sql
  </read_first>
  <files>supabase/migrations/20260513_22_mock_exam_system.sql</files>
  <action>
    Apply migration via Supabase workflow (`npx supabase db push` or project standard equivalent). If CLI is unavailable/unlinked, provide blocking checkpoint with exact SQL-execution steps in Supabase SQL Editor and required verification queries.
    Verify post-apply:
    - tables exist,
    - RPCs exist,
    - answer table has no student SELECT.
  </action>
  <verify>
    <automated>grep -E "CREATE TABLE.*exam_sessions|CREATE OR REPLACE FUNCTION public.submit_exam_attempt" supabase/migrations/20260513_22_mock_exam_system.sql | wc -l | grep -q "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - Migration is applied or checkpoint with explicit manual steps is recorded.
    - Verification query output is included in summary.
    - No unresolved blockers for API plan start.
  </acceptance_criteria>
  <done>Database contracts are live and verified for downstream plans.</done>
</task>

</tasks>

<verification>
Run schema lint by parsing migration and ensuring required functions/tables/constraints are present. Confirm no student-answer leakage path exists in policies.
</verification>

<success_criteria>
Phase 18 DB contracts are ready: admin can publish valid sessions only, student attempts are single-shot, late submit is server-rejected, and grading results can be returned immediately without exposing canonical answers.
</success_criteria>

<output>
After completion, create `.planning/phases/18-mock-exam-system/18-01-SUMMARY.md`.
</output>
