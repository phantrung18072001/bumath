---
phase: 13-student-pages
plan: 01
type: execute
wave: 1
depends_on:
  - "13-00"
files_modified:
  - src/pages/student/CoursesPage.tsx
  - src/components/student/LessonSidebar.tsx
autonomous: true
requirements:
  - STUDENT-UI-01
  - STUDENT-UI-04
  - DS-02
must_haves:
  truths:
    - "CoursesPage cards use Claymorphism styling with teal border"
    - "Progress bars display with h-3 height and teal fill color"
    - "Progress label shows '{N}% hoàn thành' format"
    - "Empty state has BookOpen icon, correct heading, and CTA to /danh-muc"
    - "Course card links navigate to /khoa-hoc/{slug} (URL bug fixed)"
    - "Loading shows 4 skeleton cards with rounded-3xl"
    - "Error state uses plain <p> not <Alert>"
  artifacts:
    - path: "src/pages/student/CoursesPage.tsx"
      provides: "Student courses page with Claymorphism cards and polished empty state"
      contains: "bm-clay-card-student"
    - path: "src/pages/student/CoursesPage.tsx"
      provides: "Fixed URL links to Vietnamese paths"
      contains: "/khoa-hoc/${course.slug}"
    - path: "src/components/student/LessonSidebar.tsx"
      provides: "Teal progress bar in sidebar"
      contains: "bm-progress-teal"
  key_links:
    - from: "src/pages/student/CoursesPage.tsx"
      to: "/khoa-hoc/{slug}"
      via: "Link to prop"
      pattern: "to=.*khoa-hoc"
    - from: "src/pages/student/CoursesPage.tsx"
      to: "/danh-muc"
      via: "Empty state CTA"
      pattern: "to=\"/danh-muc\""
---

<objective>
Refactor CoursesPage with Claymorphism design, enhanced progress bars, polished empty state, and fix the URL bug.

Purpose: Transform the enrolled courses list into a polished student experience with teal Claymorphism cards, prominent teal progress bars, and a helpful empty state that directs users to the catalogue.

Output:
- `src/pages/student/CoursesPage.tsx` — Full Claymorphism refactor with all visual upgrades
- `src/components/student/LessonSidebar.tsx` — Teal progress bar override
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-student-pages/13-CONTEXT.md
@.planning/phases/13-student-pages/13-UI-SPEC.md

@src/pages/student/CoursesPage.tsx
@src/components/student/LessonSidebar.tsx
@src/index.css (for CSS class reference)
</context>

<interfaces>
<!-- CSS classes from Wave 0 -->
From src/index.css (added in 13-00):
```css
.bm-clay-card-student { background: #FFFFFF; border: 3px solid #0D9488; ... }
.bm-progress-teal [data-slot="progress-indicator"], .bm-progress-teal > div { background-color: #0D9488 !important; }
```

<!-- Current CoursesPage structure -->
From src/pages/student/CoursesPage.tsx:
```tsx
// Line 118 — URL BUG to fix:
to={`/courses/${course.slug}`}  // WRONG — should be /khoa-hoc/

// Line 121 — Current card (no Claymorphism):
<Card className="hover:shadow-md transition-shadow cursor-pointer h-full">

// Lines 133-137 — Current progress bar:
<Progress value={progress} className="h-2 mt-2 bg-muted" />
```

<!-- shadcn imports needed -->
- Card, CardHeader, CardContent, CardTitle from '@/components/ui/card'
- Progress from '@/components/ui/progress'
- Badge from '@/components/ui/badge'
- Skeleton from '@/components/ui/skeleton'
- Button from '@/components/ui/button'
- BookOpen from 'lucide-react' (for empty state icon)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor CoursesPage — Claymorphism cards, progress, empty state, URL fix</name>
  <files>src/pages/student/CoursesPage.tsx</files>
  <read_first>
    - src/pages/student/CoursesPage.tsx (current state — all 151 lines)
    - .planning/phases/13-student-pages/13-UI-SPEC.md (lines 175-257 for CoursesPage specs)
    - src/index.css (verify CSS classes exist from Wave 0)
  </read_first>
  <action>
Apply these changes to `src/pages/student/CoursesPage.tsx`:

**1. Add BookOpen import** (top of file, with lucide imports):
```tsx
import { BookOpen } from 'lucide-react'
```

**2. Add Button import** (if not present):
```tsx
import { Button } from '@/components/ui/button'
```

**3. Remove Alert import** (lines 14 — not needed after error state change):
```tsx
// DELETE: import { Alert, AlertDescription } from '@/components/ui/alert'
```

**4. Update page heading** (line 70):
```tsx
// OLD: <h1 className="text-2xl font-semibold mb-6">Khóa học của tôi</h1>
// NEW:
<h1 className="text-2xl font-bold mb-6 text-[#134E4A]">Khóa học của tôi</h1>
```

**5. Replace error state** (lines 73-79):
```tsx
// OLD: <Alert variant="destructive">...</Alert>
// NEW:
{enrollmentsError && (
  <p className="text-destructive text-center py-8">
    Không thể tải dữ liệu. Vui lòng thử lại.
  </p>
)}
```

**6. Update skeleton loading** (lines 82-87):
```tsx
{isLoading && (
  <div className="grid grid-cols-1 gap-6 sm:grid-cols-2">
    {Array.from({ length: 4 }).map((_, i) => (
      <Skeleton key={i} className="h-36 rounded-3xl" />
    ))}
  </div>
)}
```

**7. Replace empty state** (lines 91-102):
```tsx
{!isLoading && !enrollmentsError && enrollments?.length === 0 && (
  <div className="flex flex-col items-center justify-center py-16 gap-4 text-center">
    <BookOpen className="h-16 w-16 text-[#0D9488]" aria-hidden="true" />
    <h2 className="text-xl font-bold text-[#134E4A]">Bạn chưa có khóa học nào</h2>
    <p className="text-base text-muted-foreground max-w-sm">
      Liên hệ giảng viên để được thêm vào khóa học, hoặc khám phá danh mục.
    </p>
    <Link to="/danh-muc">
      <Button className="bm-btn-cta min-h-[48px] px-6">
        Khám phá khóa học
      </Button>
    </Link>
  </div>
)}
```

**8. Fix URL bug on line 118** — change `/courses/` to `/khoa-hoc/`:
```tsx
// OLD: to={`/courses/${course.slug}`}
// NEW:
to={`/khoa-hoc/${course.slug}`}
```

**9. Update Card to Claymorphism** (line 121):
```tsx
// OLD: <Card className="hover:shadow-md transition-shadow cursor-pointer h-full">
// NEW:
<Card className="bm-clay-card-student border-0 shadow-none p-0 overflow-hidden h-full">
```

**10. Update CardTitle styling** (line 124):
```tsx
// OLD: <CardTitle className="text-xl font-semibold leading-snug">
// NEW:
<CardTitle className="text-xl font-bold leading-snug text-[#134E4A]">
```

**11. Update Progress bar** (lines 133-137):
```tsx
// OLD: <Progress value={progress} className="h-2 mt-2 bg-muted" />
// NEW:
<Progress
  value={progress}
  className="h-3 mt-2 bg-[#CCFBF1] bm-progress-teal"
  aria-label={`Tiến độ hoàn thành: ${progress}%`}
/>
```

**12. Update progress label styling** (line 138):
```tsx
// OLD: <span className="text-sm text-muted-foreground mt-1 block">
// NEW:
<span className="text-sm text-muted-foreground mt-2 block">
```

**12b. Update progress label text content** (per D-05 — ensure format is `{progress}% hoàn thành`):
```tsx
// Ensure the span content displays the correct Vietnamese format:
<span className="text-sm text-muted-foreground mt-2 block">
  {progress}% hoàn thành
</span>
```
If the existing code has a different format (e.g. just `{progress}%` or `{progress}% complete`), change the text to `{progress}% hoàn thành`.
  </action>
  <verify>
    <automated>grep -n "bm-clay-card-student" src/pages/student/CoursesPage.tsx && grep -n "khoa-hoc/\${course.slug}" src/pages/student/CoursesPage.tsx && grep -n "BookOpen" src/pages/student/CoursesPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CoursesPage.tsx contains `bm-clay-card-student` class on Card
    - CoursesPage.tsx contains `to={\`/khoa-hoc/${course.slug}\`}` (URL bug fixed)
    - CoursesPage.tsx imports and renders `BookOpen` icon in empty state
    - CoursesPage.tsx contains `bm-progress-teal` class on Progress
    - CoursesPage.tsx contains `h-3` on Progress (not h-2)
    - CoursesPage.tsx contains `bg-[#CCFBF1]` Progress track color
    - Empty state heading is "Bạn chưa có khóa học nào"
    - Empty state CTA links to `/danh-muc`
    - Error state uses `<p className="text-destructive"` not `<Alert>`
    - No import of `Alert, AlertDescription`
  </acceptance_criteria>
  <done>CoursesPage fully refactored with Claymorphism cards, teal progress bars, polished empty state, and URL bug fixed</done>
</task>

<task type="auto">
  <name>Task 2: Update LessonSidebar progress bar to teal</name>
  <files>src/components/student/LessonSidebar.tsx</files>
  <read_first>
    - src/components/student/LessonSidebar.tsx (current state — lines 31-36 for Progress)
    - .planning/phases/13-student-pages/13-UI-SPEC.md (lines 479-484)
  </read_first>
  <action>
In `src/components/student/LessonSidebar.tsx`, update the Progress component in the progress header section (lines 31-36):

**1. Update Progress className** (line 32):
```tsx
// OLD: <Progress value={progress} className="h-2 bg-muted" aria-label={...} />
// NEW:
<Progress
  value={progress}
  className="h-2 bg-[#CCFBF1] bm-progress-teal"
  aria-label={`Tiến độ hoàn thành: ${progress}%`}
/>
```

**2. Update border color** (line 30):
```tsx
// OLD: <div className="px-4 py-4 border-b border-sidebar-border shrink-0">
// NEW:
<div className="px-4 py-4 border-b border-[#0D9488]/20 shrink-0">
```
  </action>
  <verify>
    <automated>grep -n "bm-progress-teal" src/components/student/LessonSidebar.tsx && grep -n "bg-\[#CCFBF1\]" src/components/student/LessonSidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LessonSidebar.tsx contains `bm-progress-teal` class on Progress
    - LessonSidebar.tsx contains `bg-[#CCFBF1]` Progress track color
    - LessonSidebar.tsx contains `border-[#0D9488]/20` on header div
  </acceptance_criteria>
  <done>LessonSidebar progress bar uses teal fill via bm-progress-teal class</done>
</task>

</tasks>

<verification>
Run after all tasks:
```bash
# Verify CoursesPage changes
grep -c "bm-clay-card-student" src/pages/student/CoursesPage.tsx  # Should be 1
grep "khoa-hoc" src/pages/student/CoursesPage.tsx | grep -v "Khóa"  # Should show /khoa-hoc/ link
grep "BookOpen" src/pages/student/CoursesPage.tsx  # Should show import and usage

# Verify LessonSidebar changes
grep "bm-progress-teal" src/components/student/LessonSidebar.tsx

# Run tests
yarn test src/pages/student/CoursesPage.test.tsx --run
```
</verification>

<success_criteria>
- [ ] CoursesPage cards use `.bm-clay-card-student` class with `border-0 shadow-none`
- [ ] Progress bars have `h-3 bg-[#CCFBF1] bm-progress-teal` classes
- [ ] Progress label shows `{progress}% hoàn thành`
- [ ] Empty state has BookOpen icon (h-16 w-16), correct heading, body text, and CTA button
- [ ] CTA button uses `.bm-btn-cta` class and links to `/danh-muc`
- [ ] URL bug fixed: links to `/khoa-hoc/${course.slug}` not `/courses/`
- [ ] Error state uses `<p className="text-destructive">` not `<Alert>`
- [ ] Skeleton cards have `rounded-3xl` class
- [ ] LessonSidebar Progress has `bm-progress-teal` class
- [ ] `yarn test src/pages/student/CoursesPage.test.tsx` passes heading test
</success_criteria>

<output>
After completion, create `.planning/phases/13-student-pages/13-01-SUMMARY.md`
</output>
