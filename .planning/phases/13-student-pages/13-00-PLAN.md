---
phase: 13-student-pages
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/index.css
  - src/pages/student/CoursesPage.test.tsx
  - src/components/student/StudentLayout.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
must_haves:
  truths:
    - ".bm-clay-card-student CSS class exists with teal border and double shadow"
    - ".bm-progress-teal CSS class targets progress indicator with teal fill"
    - "CoursesPage test file exists with stubs for all STUDENT-UI-01 behaviors"
    - "StudentLayout main has bg-[#F0FDFA] page background"
  artifacts:
    - path: "src/index.css"
      provides: "Claymorphism student card + teal progress CSS classes"
      contains: ".bm-clay-card-student"
    - path: "src/index.css"
      provides: "Teal progress bar indicator override"
      contains: ".bm-progress-teal"
    - path: "src/pages/student/CoursesPage.test.tsx"
      provides: "Test stubs for CoursesPage refactor"
      contains: "describe('CoursesPage'"
    - path: "src/components/student/StudentLayout.tsx"
      provides: "Updated page background color"
      contains: "bg-[#F0FDFA]"
  key_links:
    - from: "src/pages/student/CoursesPage.tsx"
      to: "src/index.css"
      via: "bm-clay-card-student class"
      pattern: "bm-clay-card-student"
---

<objective>
Create Phase 13 CSS foundation and test infrastructure.

Purpose: Wave 0 establishes the CSS classes (.bm-clay-card-student, .bm-progress-teal), font update, page background, and test stubs needed before Wave 1 page refactors.

Output: 
- `src/index.css` with new CSS classes and font imports
- `src/pages/student/CoursesPage.test.tsx` with test stubs
- `src/components/student/StudentLayout.tsx` with bg fix
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-student-pages/13-CONTEXT.md
@.planning/phases/13-student-pages/13-UI-SPEC.md
@.planning/phases/13-student-pages/13-RESEARCH.md

@src/index.css
@src/components/student/StudentLayout.tsx
@src/pages/Login.tsx (Claymorphism reference)
</context>

<interfaces>
<!-- Existing CSS classes in src/index.css to maintain pattern with -->
From src/index.css:
```css
.bm-clay-card {
  background: var(--bm-card);
  border: 3px solid var(--bm-border);
  border-radius: 24px;
  box-shadow: 0 8px 0 var(--bm-border), 0 12px 24px rgba(249, 115, 22, 0.10);
  ...
}

.bm-btn-cta {
  background: var(--bm-cta) !important;
  color: #ffffff;
  ...
}
```

From src/components/student/StudentLayout.tsx:
```tsx
<main className="min-h-[calc(100vh-48px)] bg-background">
  {children}
</main>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS classes and font imports to index.css</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (current state — see existing .bm-clay-card pattern)
    - .planning/phases/13-student-pages/13-UI-SPEC.md (CSS specs lines 43-73, 516-536)
  </read_first>
  <action>
Add these CSS rules to `src/index.css`:

1. **Replace the Google Fonts import** (line 1):
   - OLD: `@import url('https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;500;600;700;800;900&display=swap');`
   - NEW: `@import url('https://fonts.googleapis.com/css2?family=Baloo+2:wght@400;700&family=Comic+Neue:wght@400;700&family=Be+Vietnam+Pro:wght@400;500;600;700;800;900&display=swap');`
   (Keep Be Vietnam Pro for admin/landing pages, add Baloo 2 + Comic Neue for student pages — scoped via font-family on specific elements, not global body change)

2. **Add `.bm-clay-card-student` class** (after the existing `.bm-clay-card` block, around line 17):
```css
/* === BuMath Student Claymorphism Card (Teal) === */
.bm-clay-card-student {
  background: #FFFFFF;
  border: 3px solid #0D9488;
  border-radius: 24px;
  box-shadow:
    0 8px 0 #0D9488,
    0 2px 0 rgba(255, 255, 255, 0.8) inset;
  transition: box-shadow 200ms ease, transform 200ms ease;
  cursor: pointer;
}

.bm-clay-card-student:hover {
  transform: translateY(-2px);
  box-shadow:
    0 10px 0 #0D9488,
    0 2px 0 rgba(255, 255, 255, 0.8) inset;
}

@media (prefers-reduced-motion: reduce) {
  .bm-clay-card-student {
    transition: none;
  }
  .bm-clay-card-student:hover {
    transform: none;
  }
}
```

3. **Add `.bm-progress-teal` class** (after the card class):
```css
/* === BuMath Teal Progress Bar Override === */
.bm-progress-teal [data-slot="progress-indicator"],
.bm-progress-teal > div {
  background-color: #0D9488 !important;
}
```
  </action>
  <verify>
    <automated>grep -n "bm-clay-card-student" src/index.css && grep -n "bm-progress-teal" src/index.css && grep -n "Baloo+2" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains `.bm-clay-card-student {` with `border: 3px solid #0D9488`
    - src/index.css contains `.bm-clay-card-student:hover` with `transform: translateY(-2px)`
    - src/index.css contains `@media (prefers-reduced-motion: reduce)` block for `.bm-clay-card-student`
    - src/index.css contains `.bm-progress-teal` with `background-color: #0D9488 !important`
    - src/index.css @import contains `Baloo+2` and `Comic+Neue` fonts
  </acceptance_criteria>
  <done>CSS classes .bm-clay-card-student and .bm-progress-teal exist with exact specs from UI-SPEC; fonts imported</done>
</task>

<task type="auto">
  <name>Task 2: Update StudentLayout page background</name>
  <files>src/components/student/StudentLayout.tsx</files>
  <read_first>
    - src/components/student/StudentLayout.tsx (current state — line 92 has `bg-background`)
    - .planning/phases/13-student-pages/13-UI-SPEC.md (lines 541-547)
  </read_first>
  <action>
In `src/components/student/StudentLayout.tsx`, change line 92:

OLD:
```tsx
<main className="min-h-[calc(100vh-48px)] bg-background">
```

NEW:
```tsx
<main className="min-h-[calc(100vh-48px)] bg-[#F0FDFA]">
```

This sets the mint-teal background for all student pages wrapped in StudentLayout (CoursesPage, CourseDetailPage enrolled, CataloguePage authenticated).
  </action>
  <verify>
    <automated>grep -n "bg-\[#F0FDFA\]" src/components/student/StudentLayout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/student/StudentLayout.tsx main element has `bg-[#F0FDFA]` class
    - No `bg-background` remains on the main element
  </acceptance_criteria>
  <done>StudentLayout main has bg-[#F0FDFA] mint background per DS-01</done>
</task>

<task type="auto">
  <name>Task 3: Create CoursesPage test scaffold</name>
  <files>src/pages/student/CoursesPage.test.tsx</files>
  <read_first>
    - src/pages/student/CoursesPage.tsx (component structure for mocking)
    - src/pages/student/CataloguePage.test.tsx (test pattern reference)
    - .planning/phases/13-student-pages/13-VALIDATION.md (Wave 0 requirements)
  </read_first>
  <action>
Create `src/pages/student/CoursesPage.test.tsx` with test stubs for STUDENT-UI-01 and DS-02:

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import { BrowserRouter } from 'react-router-dom'
import CoursesPage from './CoursesPage'

// Mock AuthContext
vi.mock('@/contexts/AuthContext', () => ({
  useAuth: vi.fn(() => ({
    profile: { id: 'test-user-id', full_name: 'Test User', role: 'student' },
    user: { id: 'test-user-id' },
    loading: false,
    signOut: vi.fn(),
  })),
}))

// Mock TanStack Query
vi.mock('@tanstack/react-query', async () => {
  const actual = await vi.importActual('@tanstack/react-query')
  return {
    ...actual,
    useQuery: vi.fn(() => ({
      data: [],
      isLoading: false,
      isError: false,
    })),
  }
})

function renderCoursesPage() {
  return render(
    <BrowserRouter>
      <CoursesPage />
    </BrowserRouter>
  )
}

describe('CoursesPage', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  describe('STUDENT-UI-01: Card layout with progress bars', () => {
    it('renders page heading "Khóa học của tôi"', () => {
      renderCoursesPage()
      expect(screen.getByRole('heading', { name: /khóa học của tôi/i })).toBeInTheDocument()
    })

    it.todo('renders course cards with bm-clay-card-student class')
    it.todo('renders progress bar with h-3 height and teal fill')
    it.todo('renders progress label with format "{N}% hoàn thành"')
    it.todo('links to /khoa-hoc/{slug} not /courses/{slug}')
  })

  describe('STUDENT-UI-04: Empty state', () => {
    it.todo('renders empty state with BookOpen icon when no courses')
    it.todo('renders "Bạn chưa có khóa học nào" heading in empty state')
    it.todo('renders CTA button linking to /danh-muc')
  })

  describe('DS-02: Loading skeleton', () => {
    it.todo('renders 4 skeleton cards with rounded-3xl during loading')
  })

  describe('Error state', () => {
    it.todo('renders error message as plain <p> not <Alert>')
  })
})
```
  </action>
  <verify>
    <automated>yarn test src/pages/student/CoursesPage.test.tsx --run 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/student/CoursesPage.test.tsx` exists
    - Contains `describe('CoursesPage'` block
    - Contains `describe('STUDENT-UI-01` block with card/progress tests
    - Contains `describe('STUDENT-UI-04` block with empty state tests
    - Contains `describe('DS-02` block with skeleton tests
    - First test (page heading) passes; other tests are `.todo()` stubs
  </acceptance_criteria>
  <done>CoursesPage test scaffold exists with passing heading test and todo stubs for Phase 13 behaviors</done>
</task>

</tasks>

<verification>
Run after all tasks:
```bash
# Verify CSS classes added
grep -c "bm-clay-card-student" src/index.css  # Should be >= 3 (class, hover, reduced-motion)
grep -c "bm-progress-teal" src/index.css      # Should be >= 1

# Verify StudentLayout bg
grep "bg-\[#F0FDFA\]" src/components/student/StudentLayout.tsx

# Verify tests run
yarn test src/pages/student/CoursesPage.test.tsx --run
```
</verification>

<success_criteria>
- [ ] `.bm-clay-card-student` CSS class exists with teal border (#0D9488), double shadow, and reduced-motion support
- [ ] `.bm-progress-teal` CSS class exists targeting progress indicator with teal fill
- [ ] Font import includes Baloo 2 and Comic Neue alongside Be Vietnam Pro
- [ ] StudentLayout `<main>` has `bg-[#F0FDFA]` instead of `bg-background`
- [ ] CoursesPage.test.tsx exists with test stubs for STUDENT-UI-01, STUDENT-UI-04, DS-02
- [ ] `yarn test src/pages/student/CoursesPage.test.tsx` runs without fatal errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-student-pages/13-00-SUMMARY.md`
</output>
