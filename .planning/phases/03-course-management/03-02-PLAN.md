---
phase: 03-course-management
plan: 02
wave: 2
execute_via: code
autonomous: true
gap_closure: false
---

<objective>
To build the top-level Admin Courses page containing the list of all courses and the Dialog to create/edit courses, fulfilling COURSE-01 requirements.
</objective>

<context>
- This relies on the table `courses`.
- Uses Tanstack Query for data fetching and React Hook Form + Zod for forms.
- Re-uses `shadcn/ui` components as specified in `03-UI-SPEC.md`.
</context>

<tasks>
1. Setup Course API calls
   - Create `src/lib/api/courses.ts` with Supabase fetch, insert, update, delete methods.
2. Setup Admin Courses Page UI Outline
   - Create `src/pages/admin/CoursesPage.tsx`.
   - Add routing for `/admin/courses` in `App.tsx` (wrapped in ProtectedRoute for admin).
   - Implement empty state and loading state per UI-SPEC.
3. Build Course List Table
   - Fetch courses and populate a `Table`.
   - Use grade badges (Lớp 7, 8, 9, Ôn chuyên) styled according to UI-SPEC semantic colors.
   - Add Edit and Delete actions in the final column. Use `AlertDialog` for Delete.
4. Build Create/Edit Course Dialog
   - Create a separate component `CourseFormDialog.tsx`.
   - Use RHF + Zod for validation of: title, description, target_grade.
   - Implement mutations with success/error toast notifications.
</tasks>
