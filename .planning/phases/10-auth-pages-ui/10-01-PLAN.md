---
phase: 10-auth-pages-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - tailwind.config.ts
autonomous: true
requirements:
  - DS-01
user_setup: []

must_haves:
  truths:
    - "BuMath CSS variables are available in :root for auth pages"
    - "Google Fonts (Baloo 2, Comic Neue) load correctly"
    - "Tailwind font-baloo and font-comic classes work"
    - "Claymorphism card class (.bm-clay-card) applies thick border and double shadow"
    - "Math symbol float animation class (.bm-float-symbol) animates translateY"
    - "CTA button class (.bm-btn-cta) has orange background"
  artifacts:
    - path: "src/index.css"
      provides: "BuMath design system CSS: variables, .bm-clay-card, .bm-float-symbol, .bm-btn-cta, @keyframes bm-float"
      contains: "--bm-primary: #0D9488"
    - path: "tailwind.config.ts"
      provides: "Tailwind fontFamily extensions for Baloo 2 and Comic Neue"
      contains: "fontFamily"
  key_links:
    - from: "src/index.css"
      to: "Tailwind utilities"
      via: "CSS custom classes after @tailwind utilities"
      pattern: "@tailwind utilities"
    - from: "tailwind.config.ts"
      to: "src/pages/Login.tsx"
      via: "font-baloo, font-comic Tailwind classes"
      pattern: "font-baloo"
---

<objective>
Add BuMath v2 design system CSS foundation (custom properties, utility classes, animations) and Tailwind fontFamily extensions.

Purpose: Establish the design system foundation for auth pages — CSS variables, Claymorphism card class, CTA button style, float animation keyframes, and Tailwind font utilities.

Output: Updated `src/index.css` with BuMath CSS additions; updated `tailwind.config.ts` with fontFamily extensions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-auth-pages-ui/10-CONTEXT.md
@.planning/phases/10-auth-pages-ui/10-UI-SPEC.md
@.planning/phases/10-auth-pages-ui/10-RESEARCH.md

<interfaces>
<!-- Source: 10-UI-SPEC.md § CSS Additions Required -->
<!-- Executor MUST add these exact CSS additions — do NOT modify existing shadcn variables -->

Google Fonts import (add at TOP of file, before @tailwind base):
```css
@import url('https://fonts.googleapis.com/css2?family=Baloo+2:wght@400;700&family=Comic+Neue:wght@400;700&display=swap');
```

CSS custom properties (ADD to :root, do NOT replace existing):
```css
--bm-primary: #0D9488;
--bm-secondary: #2DD4BF;
--bm-cta: #F97316;
--bm-bg: #F0FDFA;
--bm-text: #134E4A;
--bm-card: #FFFFFF;
--bm-border: #0D9488;
--bm-muted: #5EADA5;
```

Utility classes (add AFTER @tailwind utilities):
- `.bm-clay-card` — thick border, double shadow, rounded-3xl
- `@keyframes bm-float` — translateY 0→-12px→0
- `.bm-float-symbol` — absolute, animated, opacity 0.08
- `.bm-btn-cta` — orange #F97316 background with hover/disabled states

Tailwind fontFamily extension (tailwind.config.ts):
```ts
fontFamily: {
  'baloo': ['"Baloo 2"', 'sans-serif'],
  'comic': ['"Comic Neue"', 'cursive'],
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BuMath CSS to index.css</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (current state — preserve all existing CSS)
    - .planning/phases/10-auth-pages-ui/10-UI-SPEC.md §CSS Additions Required (exact CSS to add)
  </read_first>
  <action>
Add BuMath design system CSS to `src/index.css`. The file structure must be:

1. **Line 1 (NEW):** Add Google Fonts import BEFORE the existing Be Vietnam Pro import:
```css
@import url('https://fonts.googleapis.com/css2?family=Baloo+2:wght@400;700&family=Comic+Neue:wght@400;700&display=swap');
```

2. **Inside existing `:root { ... }` block:** Add 8 BuMath CSS custom properties AFTER the existing shadcn variables (do NOT remove or modify any existing `--background`, `--primary`, etc. variables):
```css
/* === BuMath Design System v2 === */
--bm-primary: #0D9488;
--bm-secondary: #2DD4BF;
--bm-cta: #F97316;
--bm-bg: #F0FDFA;
--bm-text: #134E4A;
--bm-card: #FFFFFF;
--bm-border: #0D9488;
--bm-muted: #5EADA5;
```

3. **AFTER `@tailwind utilities;` line:** Add all utility classes (approximately 50 lines of CSS):

```css
/* === BuMath Claymorphism Card === */
.bm-clay-card {
  background: var(--bm-card);
  border: 3px solid var(--bm-border);
  border-radius: 24px;
  box-shadow:
    0 8px 0 var(--bm-border),
    0 12px 24px rgba(13, 148, 136, 0.10);
  padding: 32px;
  transition: box-shadow 200ms ease, transform 200ms ease;
}

/* === BuMath Float Animation (math symbols) === */
@keyframes bm-float {
  0%   { transform: translateY(0px); }
  50%  { transform: translateY(-12px); }
  100% { transform: translateY(0px); }
}

@media (prefers-reduced-motion: reduce) {
  .bm-float-symbol { animation: none !important; }
}

.bm-float-symbol {
  position: absolute;
  font-family: 'Comic Neue', cursive;
  font-weight: 700;
  color: var(--bm-primary);
  opacity: 0.08;
  pointer-events: none;
  user-select: none;
  animation: bm-float ease-in-out infinite;
}

.bm-float-symbol:nth-child(1) { animation-duration: 3.0s; }
.bm-float-symbol:nth-child(2) { animation-duration: 4.0s; }
.bm-float-symbol:nth-child(3) { animation-duration: 5.0s; }
.bm-float-symbol:nth-child(4) { animation-duration: 3.5s; }
.bm-float-symbol:nth-child(5) { animation-duration: 4.5s; }
.bm-float-symbol:nth-child(6) { animation-duration: 6.0s; }

/* === BuMath CTA Button === */
.bm-btn-cta {
  background: var(--bm-cta) !important;
  color: #ffffff;
  border: none;
  border-radius: 8px;
  font-weight: 700;
  font-family: 'Baloo 2', sans-serif;
  min-height: 48px;
  cursor: pointer;
  transition: opacity 200ms ease, transform 200ms ease;
}
.bm-btn-cta:hover:not(:disabled) {
  opacity: 0.9;
  transform: translateY(-1px);
}
.bm-btn-cta:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  transform: none;
}
```

**CRITICAL:** Do NOT modify, remove, or reorder ANY existing CSS. The shadcn variables (--background, --primary, etc.) and Be Vietnam Pro import are used by other pages.
  </action>
  <verify>
    <automated>grep -q "bm-primary: #0D9488" src/index.css && grep -q "bm-clay-card" src/index.css && grep -q "@keyframes bm-float" src/index.css && grep -q "bm-btn-cta" src/index.css && grep -q "Baloo+2" src/index.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` line 1 contains `@import url('https://fonts.googleapis.com/css2?family=Baloo+2`
    - `src/index.css` contains `--bm-primary: #0D9488;`
    - `src/index.css` contains `--bm-cta: #F97316;`
    - `src/index.css` contains `.bm-clay-card {`
    - `src/index.css` contains `@keyframes bm-float {`
    - `src/index.css` contains `@media (prefers-reduced-motion: reduce)`
    - `src/index.css` contains `.bm-float-symbol {`
    - `src/index.css` contains `.bm-btn-cta {`
    - Existing `--background:`, `--primary:`, `--card:` variables are still present (not removed)
    - Existing `Be Vietnam Pro` import is still present
  </acceptance_criteria>
  <done>BuMath CSS variables (8 --bm-* vars), .bm-clay-card, @keyframes bm-float, .bm-float-symbol, .bm-btn-cta all present in src/index.css; existing shadcn CSS untouched</done>
</task>

<task type="auto">
  <name>Task 2: Add Tailwind fontFamily extensions</name>
  <files>tailwind.config.ts</files>
  <read_first>
    - tailwind.config.ts (current state — preserve all existing config)
    - .planning/phases/10-auth-pages-ui/10-UI-SPEC.md §Typography (font values)
  </read_first>
  <action>
Add `fontFamily` entries to `tailwind.config.ts` inside `theme.extend`:

1. Locate the `extend: {` block (around line 17)
2. Add `fontFamily` object with `baloo` and `comic` entries:

```ts
extend: {
  fontFamily: {
    'baloo': ['"Baloo 2"', 'sans-serif'],
    'comic': ['"Comic Neue"', 'cursive'],
  },
  colors: {
    // ... existing colors ...
  },
```

This enables Tailwind classes `font-baloo` and `font-comic` for use in auth pages.

**Do NOT remove or modify any existing config.** The colors, borderRadius, keyframes, animation, and plugins must all remain intact.
  </action>
  <verify>
    <automated>grep -q "fontFamily" tailwind.config.ts && grep -q "'baloo'" tailwind.config.ts && grep -q "'comic'" tailwind.config.ts && grep -q "Baloo 2" tailwind.config.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `tailwind.config.ts` contains `fontFamily: {`
    - `tailwind.config.ts` contains `'baloo': ['"Baloo 2"', 'sans-serif']`
    - `tailwind.config.ts` contains `'comic': ['"Comic Neue"', 'cursive']`
    - Existing `colors`, `borderRadius`, `keyframes`, `animation` blocks are unchanged
    - File is valid TypeScript (no syntax errors)
  </acceptance_criteria>
  <done>Tailwind config has fontFamily.baloo and fontFamily.comic; `font-baloo` and `font-comic` classes work; existing config preserved</done>
</task>

</tasks>

<verification>
After both tasks:
1. `yarn test` passes (no regressions)
2. `yarn dev` starts without errors
3. Browser DevTools shows `--bm-primary` in `:root`
4. Tailwind classes `font-baloo`, `font-comic` are recognized (no build warnings)
</verification>

<success_criteria>
- src/index.css contains all 8 --bm-* CSS custom properties
- src/index.css contains .bm-clay-card, .bm-float-symbol, .bm-btn-cta utility classes
- src/index.css contains @keyframes bm-float and prefers-reduced-motion media query
- tailwind.config.ts has fontFamily.baloo and fontFamily.comic
- Existing shadcn CSS and Tailwind config completely preserved
- `yarn test` green
</success_criteria>

<output>
After completion, create `.planning/phases/10-auth-pages-ui/10-01-SUMMARY.md`
</output>
