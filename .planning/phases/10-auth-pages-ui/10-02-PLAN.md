---
phase: 10-auth-pages-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/pages/Login.tsx
  - src/pages/Register.tsx
autonomous: true
requirements:
  - AUTH-UI-01
  - AUTH-UI-02
  - AUTH-UI-03  # Descoped/removed per D-05 — no Pending.tsx; documented here for traceability
user_setup: []

must_haves:
  truths:
    - "Login page has BuMath logo (image + text) centered above card"
    - "Login page uses Claymorphism card with teal border and double shadow"
    - "Login page has floating math symbols (π √ ± × ÷ ∑) in background"
    - "Login CTA button is orange (#F97316), not teal"
    - "Register page has 2-column grid on desktop (sm:grid-cols-2), single column on mobile"
    - "Register page uses same Claymorphism card and logo as Login"
    - "Both pages have #F0FDFA background color"
    - "All existing auth logic (useState, RHF+Zod, supabase, redirects) is preserved verbatim"
  artifacts:
    - path: "src/pages/Login.tsx"
      provides: "Refactored login page with Claymorphism design"
      contains: "bm-clay-card"
    - path: "src/pages/Register.tsx"
      provides: "Refactored register page with 2-column grid layout"
      contains: "sm:grid-cols-2"
  key_links:
    - from: "src/pages/Login.tsx"
      to: "src/index.css"
      via: "bm-clay-card, bm-float-symbol, bm-btn-cta CSS classes"
      pattern: "bm-clay-card"
    - from: "src/pages/Register.tsx"
      to: "src/index.css"
      via: "bm-clay-card, bm-float-symbol, bm-btn-cta CSS classes"
      pattern: "bm-clay-card"
    - from: "src/pages/Login.tsx"
      to: "public/bumath.jpeg"
      via: "img src='/bumath.jpeg'"
      pattern: "src=\"/bumath.jpeg\""
---

<objective>
Refactor Login and Register pages with BuMath Claymorphism design per UI-SPEC.md.

Purpose: Transform auth pages from plain shadcn cards to polished Claymorphism design with floating math symbols, BuMath logo, and orange CTA buttons — while preserving ALL existing auth logic (useState, RHF+Zod, supabase calls, useEffect redirects).

Output: Updated `src/pages/Login.tsx` and `src/pages/Register.tsx` with new visual design.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-auth-pages-ui/10-CONTEXT.md
@.planning/phases/10-auth-pages-ui/10-UI-SPEC.md
@.planning/phases/10-auth-pages-ui/10-RESEARCH.md

<interfaces>
<!-- Source: 10-UI-SPEC.md § Shared Page Structure + Login/Register Card Interior -->
<!-- Executor MUST preserve ALL existing logic — only JSX structure and className values change -->

Page wrapper structure (BOTH pages):
```jsx
<div className="relative min-h-screen overflow-hidden" style={{ background: '#F0FDFA' }}>
  {/* 6 floating math symbols */}
  <span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '8%', left: '6%', fontSize: '36px' }}>π</span>
  <span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '15%', right: '8%', fontSize: '40px' }}>√</span>
  <span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '55%', left: '4%', fontSize: '32px' }}>±</span>
  <span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '20%', right: '15%', fontSize: '44px' }}>×</span>
  <span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '70%', right: '6%', fontSize: '36px' }}>÷</span>
  <span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '40%', left: '12%', fontSize: '48px' }}>∑</span>

  <div className="relative flex flex-col items-center justify-center min-h-screen px-4 py-12">
    {/* Logo block */}
    <div className="flex items-center gap-2 mb-8">
      <img src="/bumath.jpeg" alt="BuMath logo" className="h-11 w-auto rounded-lg" />
      <span className="font-baloo text-[30px] font-bold" style={{ color: '#0D9488' }}>BuMath</span>
    </div>

    {/* Card */}
    <div className="bm-clay-card max-w-[400px] w-full">  {/* 520px for Register */}
      ...
    </div>
  </div>
</div>
```

Login h1:
```jsx
<h1 className="font-baloo text-2xl font-bold mb-6" style={{ color: '#134E4A' }}>Đăng nhập</h1>
```

Register h1:
```jsx
<h1 className="font-baloo text-2xl font-bold mb-6" style={{ color: '#134E4A' }}>Tạo tài khoản</h1>
```

CTA Button (BOTH pages):
```jsx
<Button type="submit" className="bm-btn-cta w-full" disabled={isSubmitting}>
  {isSubmitting && <Loader2 className="h-4 w-4 animate-spin mr-2" />}
  Đăng nhập  {/* or "Đăng ký" */}
</Button>
```

Footer link (BOTH pages):
```jsx
<p className="mt-4 text-center text-sm font-comic" style={{ color: '#5EADA5' }}>
  Chưa có tài khoản?{' '}
  <Link to="/dang-ky" className="font-bold hover:underline" style={{ color: '#0D9488' }}>Đăng ký</Link>
</p>
```

Register 2-column grid (3 rows):
```jsx
<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
  <FormField name="phone" ... />
  <FormField name="fullName" ... />
</div>
<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
  <FormField name="yearOfBirth" ... />
  <FormField name="address" ... />
</div>
<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
  <FormField name="password" ... />
  <FormField name="confirmPassword" ... />
</div>
```

Form labels (font-comic):
```jsx
<FormLabel className="font-comic text-base font-normal" style={{ color: '#134E4A' }}>
  Số điện thoại
</FormLabel>
```

<!-- Login uses manual labels (not FormLabel); add font-comic and color inline -->
```jsx
<label className="font-comic text-base font-normal" style={{ color: '#134E4A' }} htmlFor="phone">
  Số điện thoại
</label>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Login.tsx with Claymorphism design</name>
  <files>src/pages/Login.tsx</files>
  <read_first>
    - src/pages/Login.tsx (current implementation — preserve ALL logic: useState, useAuth, handleSubmit, supabase call, useEffect redirect)
    - .planning/phases/10-auth-pages-ui/10-UI-SPEC.md §Shared Page Structure, §Login Card Interior, §Interaction States
    - src/index.css (verify bm-clay-card, bm-float-symbol, bm-btn-cta classes exist from Plan 01)
  </read_first>
  <action>
Refactor `src/pages/Login.tsx` JSX to use BuMath Claymorphism design. **PRESERVE ALL EXISTING LOGIC VERBATIM** — only change JSX structure and className values.

**Changes to make:**

1. **Replace outer wrapper div** (lines 73-74 current: `min-h-screen flex flex-col...`):
```jsx
<div className="relative min-h-screen overflow-hidden" style={{ background: '#F0FDFA' }}>
```

2. **Add 6 floating math symbols** as first children inside wrapper (hidden on mobile to prevent overflow):
```jsx
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '8%', left: '6%', fontSize: '36px' }}>π</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '15%', right: '8%', fontSize: '40px' }}>√</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '55%', left: '4%', fontSize: '32px' }}>±</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '20%', right: '15%', fontSize: '44px' }}>×</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '70%', right: '6%', fontSize: '36px' }}>÷</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '40%', left: '12%', fontSize: '48px' }}>∑</span>
```

3. **Add centered content wrapper** after symbols:
```jsx
<div className="relative flex flex-col items-center justify-center min-h-screen px-4 py-12">
```

4. **Replace logo div** (current lines 74-78: `BuMath-X` text) with image+text logo:
```jsx
<div className="flex items-center gap-2 mb-8">
  <img src="/bumath.jpeg" alt="BuMath logo" className="h-11 w-auto rounded-lg" />
  <span className="font-baloo text-[30px] font-bold" style={{ color: '#0D9488' }}>BuMath</span>
</div>
```

5. **Replace card div** (current line 80: `rounded-2xl border bg-card...`):
```jsx
<div className="bm-clay-card max-w-[400px] w-full">
```

6. **Update h1** (current line 81):
```jsx
<h1 className="font-baloo text-2xl font-bold mb-6" style={{ color: '#134E4A' }}>Đăng nhập</h1>
```

7. **Update form labels** (lines 86 and 104) to use font-comic:
```jsx
<label className="font-comic text-base font-normal" style={{ color: '#134E4A' }} htmlFor="phone">
  Số điện thoại
</label>
```
```jsx
<label className="font-comic text-base font-normal" style={{ color: '#134E4A' }} htmlFor="password">
  Mật khẩu
</label>
```

8. **Update CTA Button** (lines 134-142) — remove `size="lg"`, add `bm-btn-cta`, add `mr-2` to loader:
```jsx
<Button type="submit" className="bm-btn-cta w-full" disabled={isSubmitting}>
  {isSubmitting && <Loader2 className="h-4 w-4 animate-spin mr-2" />}
  Đăng nhập
</Button>
```

9. **Update footer link** (lines 145-150) — font-comic, muted color, bold teal link:
```jsx
<p className="mt-4 text-center text-sm font-comic" style={{ color: '#5EADA5' }}>
  Chưa có tài khoản?{' '}
  <Link to="/dang-ky" className="font-bold hover:underline" style={{ color: '#0D9488' }}>
    Đăng ký
  </Link>
</p>
```

**CRITICAL — Preserve these EXACTLY as-is:**
- All `useState` declarations (phone, password, showPassword, phoneError, formError, isSubmitting)
- `useAuth()` hook and `useNavigate()` hook
- `useEffect` redirect logic (lines 22-34)
- `handleSubmit` function (lines 36-70)
- `isValidVnPhone`, `phoneToEmail` imports
- All form field `value`, `onChange`, `disabled` props
- Password show/hide toggle button logic
- Error message display logic (phoneError, formError)
  </action>
  <verify>
    <automated>grep -q "bm-clay-card" src/pages/Login.tsx && grep -q "bm-float-symbol" src/pages/Login.tsx && grep -q "bm-btn-cta" src/pages/Login.tsx && grep -q 'src="/bumath.jpeg"' src/pages/Login.tsx && grep -q "font-baloo" src/pages/Login.tsx && yarn test src/pages/Login.test.tsx 2>&1 | grep -q "passed" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Login.tsx` contains `className="bm-clay-card`
    - `src/pages/Login.tsx` contains 6× `bm-float-symbol` spans with π √ ± × ÷ ∑
    - `src/pages/Login.tsx` contains `src="/bumath.jpeg"` logo image
    - `src/pages/Login.tsx` contains `font-baloo` class on logo text and h1
    - `src/pages/Login.tsx` contains `font-comic` class on labels and footer
    - `src/pages/Login.tsx` contains `bm-btn-cta` class on submit button
    - `src/pages/Login.tsx` contains `style={{ background: '#F0FDFA' }}` on outer wrapper
    - `src/pages/Login.tsx` contains `hidden sm:block` on float symbols (mobile hide)
    - All existing `useState`, `useAuth`, `useEffect`, `handleSubmit` logic unchanged
    - `yarn test src/pages/Login.test.tsx` passes (redirect logic intact)
  </acceptance_criteria>
  <done>Login.tsx uses Claymorphism card, has logo image, 6 floating symbols, orange CTA button, teal/orange colors; all tests pass; auth logic unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Register.tsx with 2-column grid layout</name>
  <files>src/pages/Register.tsx</files>
  <read_first>
    - src/pages/Register.tsx (current implementation — preserve ALL logic: RHF form, Zod schema, useAuth, onSubmit, supabase.auth.signUp, useEffect redirect)
    - .planning/phases/10-auth-pages-ui/10-UI-SPEC.md §Register Card Interior, §Responsive Breakpoints
    - .planning/phases/10-auth-pages-ui/10-CONTEXT.md D-04 (2-column grid spec)
    - src/index.css (verify CSS classes exist)
  </read_first>
  <action>
Refactor `src/pages/Register.tsx` JSX to use BuMath Claymorphism design with 2-column responsive grid. **PRESERVE ALL EXISTING LOGIC VERBATIM** — only change JSX structure and className values.

**Changes to make:**

1. **Replace outer wrapper div** (line 109 current: `min-h-screen flex flex-col...`):
```jsx
<div className="relative min-h-screen overflow-hidden" style={{ background: '#F0FDFA' }}>
```

2. **Add 6 floating math symbols** (same as Login — hidden on mobile):
```jsx
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '8%', left: '6%', fontSize: '36px' }}>π</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '15%', right: '8%', fontSize: '40px' }}>√</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '55%', left: '4%', fontSize: '32px' }}>±</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '20%', right: '15%', fontSize: '44px' }}>×</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '70%', right: '6%', fontSize: '36px' }}>÷</span>
<span className="bm-float-symbol hidden sm:block" aria-hidden="true" style={{ top: '40%', left: '12%', fontSize: '48px' }}>∑</span>
```

3. **Add centered content wrapper**:
```jsx
<div className="relative flex flex-col items-center justify-center min-h-screen px-4 py-12">
```

4. **Replace logo div** (current lines 110-114) with image+text logo:
```jsx
<div className="flex items-center gap-2 mb-8">
  <img src="/bumath.jpeg" alt="BuMath logo" className="h-11 w-auto rounded-lg" />
  <span className="font-baloo text-[30px] font-bold" style={{ color: '#0D9488' }}>BuMath</span>
</div>
```

5. **Replace card div** (current line 117: `rounded-2xl border bg-card...`) — use 520px width:
```jsx
<div className="bm-clay-card max-w-[520px] w-full">
```

6. **Update h1** (current line 118):
```jsx
<h1 className="font-baloo text-2xl font-bold mb-6" style={{ color: '#134E4A' }}>Tạo tài khoản</h1>
```

7. **Restructure form fields into 3 grid rows** (per D-04):

Row 1 — phone + fullName:
```jsx
<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
  <FormField control={form.control} name="phone" render={...existing phone render...} />
  <FormField control={form.control} name="fullName" render={...existing fullName render...} />
</div>
```

Row 2 — yearOfBirth + address:
```jsx
<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
  <FormField control={form.control} name="yearOfBirth" render={...existing yearOfBirth render...} />
  <FormField control={form.control} name="address" render={...existing address render...} />
</div>
```

Row 3 — password + confirmPassword:
```jsx
<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
  <FormField control={form.control} name="password" render={...existing password render...} />
  <FormField control={form.control} name="confirmPassword" render={...existing confirmPassword render...} />
</div>
```

8. **Update all FormLabel classes** to use font-comic:
```jsx
<FormLabel className="font-comic text-base font-normal" style={{ color: '#134E4A' }}>
  {/* field label */}
</FormLabel>
```

9. **Update CTA Button** (lines 296-304) — remove `size="lg"`, add `bm-btn-cta`, add `mr-2` to loader:
```jsx
<Button type="submit" className="bm-btn-cta w-full" disabled={isSubmitting}>
  {isSubmitting && <Loader2 className="h-4 w-4 animate-spin mr-2" />}
  Đăng ký
</Button>
```

10. **Update footer link** (lines 307-312):
```jsx
<p className="mt-4 text-center text-sm font-comic" style={{ color: '#5EADA5' }}>
  Đã có tài khoản?{' '}
  <Link to="/dang-nhap" className="font-bold hover:underline" style={{ color: '#0D9488' }}>
    Đăng nhập
  </Link>
</p>
```

**CRITICAL — Preserve these EXACTLY as-is:**
- `registerSchema` Zod schema (lines 29-46)
- `RegisterFormValues` type (line 48)
- `useForm` hook with `zodResolver` (lines 57-67)
- `useAuth()` hook and `useNavigate()` hook
- `useEffect` redirect logic (lines 72-80)
- `onSubmit` function (lines 82-107)
- `showPassword`, `showConfirmPassword` state and toggle buttons
- All `FormField` control, name, render props and validation messages
- `Select` component logic for yearOfBirth
  </action>
  <verify>
    <automated>grep -q "bm-clay-card" src/pages/Register.tsx && grep -q "sm:grid-cols-2" src/pages/Register.tsx && grep -q "bm-btn-cta" src/pages/Register.tsx && grep -q 'src="/bumath.jpeg"' src/pages/Register.tsx && grep -q "font-baloo" src/pages/Register.tsx && yarn test 2>&1 | grep -E "(passed|0 failed)" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Register.tsx` contains `className="bm-clay-card`
    - `src/pages/Register.tsx` contains `max-w-[520px]` (wider than Login)
    - `src/pages/Register.tsx` contains 3× `grid grid-cols-1 sm:grid-cols-2 gap-4` divs
    - `src/pages/Register.tsx` contains 6× `bm-float-symbol` spans
    - `src/pages/Register.tsx` contains `src="/bumath.jpeg"` logo image
    - `src/pages/Register.tsx` contains `font-baloo` class on logo text and h1
    - `src/pages/Register.tsx` contains `font-comic` class on FormLabels and footer
    - `src/pages/Register.tsx` contains `bm-btn-cta` class on submit button
    - `registerSchema` Zod schema unchanged
    - `onSubmit` supabase.auth.signUp call unchanged
    - `yarn test` passes
  </acceptance_criteria>
  <done>Register.tsx uses Claymorphism card with 520px width, 3-row 2-column grid layout, logo image, 6 floating symbols, orange CTA; all tests pass; RHF+Zod logic unchanged</done>
</task>

</tasks>

<verification>
After both tasks:
1. `yarn test` passes — all redirect and auth logic tests green
2. `yarn dev` → visit `/dang-nhap`:
   - #F0FDFA background visible
   - BuMath logo (image + text) centered above card
   - White card with thick teal border and double shadow
   - Floating math symbols animate subtly (desktop only)
   - Orange "Đăng nhập" button
   - Form validation still works
3. `yarn dev` → visit `/dang-ky`:
   - Same design system as Login
   - 2-column grid on desktop (640px+)
   - Single column on mobile (375px)
   - All 6 fields present and working
4. No console errors, no TypeScript errors
5. Mobile (375px): no horizontal scroll, single column, symbols hidden
</verification>

<success_criteria>
- Login.tsx fully refactored with Claymorphism design, logo, symbols, orange CTA
- Register.tsx fully refactored with 2-column grid, same design system
- All existing auth logic (useState, RHF+Zod, supabase, redirects) preserved verbatim
- `yarn test` green
- No regressions in functionality
- Responsive: mobile single-column, desktop 2-column for Register
</success_criteria>

<output>
After completion, create `.planning/phases/10-auth-pages-ui/10-02-SUMMARY.md`
</output>
